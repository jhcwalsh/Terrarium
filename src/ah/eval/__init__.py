"""Evaluation layer (Step 2): the validated battery, pre-registration seal, G2 rule.

``ah.eval.g2`` is the ONLY sanctioned place to mint a holdout token
(:class:`~ah.splits.FinalEvaluationToken`); no ``ah.gen`` module may import it.
"""

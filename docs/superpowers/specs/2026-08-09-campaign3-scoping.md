# Campaign-3 — scoping and the four owner rulings (2026-08-09)

*Owner: "go with your recommendations on all four and start the work."
Campaign-3 is where the extension campaign's data actually enters model
generation: retrain the generator family on the extended 813-month panel
(1953-04..2020-12, per the span-53 ratification) and re-race the extended
`bootstrap-v1` under a fresh pre-registration that hashes the seven
extension modules and their provenance artifacts. The global read-path flip
(derive/splits serving extended series) happens under campaign-3's seal,
not before — the sealed campaign-2 record keeps reproducing bit-identically
until that seal.*

**The headline:** the G2 severe test (exclude the 1970s, regenerate from
the 1965 state) was NOT POSABLE for the benchmark — its span never reached
the 1970s. Campaign-3's benchmark contains 1966-84, so the severe test is
posable for BOTH sides for the first time. A campaign-3 promotion would
mean something G2's could not.

## K1 — Holdout: future-accruing, sealed now

The one-shot holdout is SPENT (WP5.6); no clean past data exists anywhere.
Campaign-3 seals a rule that data first published after 2026-08 is
UNTOUCHABLE until a named read date (proposed: no earlier than 2029-01, one
read, the WP5.6 protocol shape). Severe tests carry the interim evidential
weight. This costs nothing today and buys the only clean test that can
ever exist again.

## K2 — Commodities: activated in campaign-3

`aqr.cmdty_ew_tr` (WP-DATA-CMDTY, ratification pending) enters via its own
`block_addition` amendment with thresholds pre-registered on the extended
reference BEFORE training. First campaign ever to see the factor; REG
licence caveats travel.

## K3 — The HAR equity_vol months: included, with a masked ablation

1973-85 equity_vol is our own reconstruction (D6-admitted). Campaign-3
trains WITH those months and runs a MASKED ablation cell (equity_vol
treated as missing pre-1986) so the circularity question becomes a
measured number, not an assumption. The ablation is named in the prereg.

## K4 — Hardware: decided before training starts

L3 flow cells measure ~2.2h each on this CPU; a campaign grid is days.
The WSL2/GPU host decision gates TRAINING ONLY — prereg drafting,
reference computation, and threshold derivation proceed on this machine.

## Order of work

1. Span-53 ratification (in flight): sealed span + amendment + re-seal.
2. Campaign-3 spec + pre-registration draft: extended reference, re-derived
   thresholds, benchmark definition, severe-test protocol (now two-sided),
   K1 holdout rule, K2 commodities block, K3 ablation cell, the read-path
   flip under the new seal.
3. Owner reviews + ratifies the campaign-3 seal; hardware call (K4).
4. Train, battery, race, verdict — the G2 machinery, on honest 1953+ data.

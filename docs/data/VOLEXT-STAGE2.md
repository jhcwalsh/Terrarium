# VOLEXT Stage 2 — the registered backcast fit and its verdict

*Generated by `scripts/volext_backcast_fit.py` at 2026-08-09T18:46:12+00:00. Thresholds
ratified as `AM-2026-08-09-001` BEFORE this fit ran; the script refuses to run
without that entry. Provenance (coefficients, HAC covariance, residual pool,
full validation): `artifacts/volext/equity-vol-backcast-provenance.json`. Ensemble paths
regenerate bit-identically from that artifact via `ah.data.vol_backcast.paths`
(owner decision D2); the median is diagnostic/display only.*

## Verdict: PASS — all registered thresholds met

| registered check | threshold | measured | pass |
|---|---|---|---|
| VXO 1986-89 held-out corr (log) | >= 0.9 | 0.9116 | True |
| Oct-1987 predicted/actual | [0.75, 1.35] | 1.038 (56.0 vs 53.9) | True |
| top-RV-decile OOS bias (log, abs) | <= 0.2 | -0.0845 | True |
| ensemble vol-of-vol ratio | >= 0.85 | 0.923 | True |
| 80% interval coverage | 0.80 +/- 0.1 | 0.775 | True |

## The fit (1990+ overlap)

- n = 438 months (1990-01-31 .. 2026-06-30), R^2 = 0.804,
  residual sigma = 0.1486 log.
- Coefficients (HAC t-stats, Newey-West Bartlett 12 lags): const +1.325 (t=14.7), log_rv22 +0.184 (t=5.6), log_rv66 +0.111 (t=2.1), log_rv252 +0.266 (t=4.9), log_maxdd +2.510 (t=7.2)
- The reference sketch's `downside` term is absent by specification (t = -1.28 on its real fit; unsupported regressors do not enter a registered spec).

## Out-of-sample (fit <= 2007, test 2008+: the GFC and COVID)

- RMSE 0.1626 log, overall bias -0.0723,
  calm-half bias -0.0743, stress-decile bias -0.0845,
  stress level ratio 1.088.
- GFC peak: predicted 66.6 vs actual 59.9 (ratio 1.11).
- COVID peak: predicted 59.5 vs actual 53.5 (ratio 1.11).
- Vol-of-vol: conditional mean retains 0.751 of the
  truth, the ensemble 0.923 — the reason the
  ensemble, never the median, feeds any tail metric.
- Expanding-window walk-forward RMSE 0.1560 log.

## The backcast

- 709 model months below the stage-1 extension (1926-12-31 .. 1985-12), ensemble n_draws=200, seed 0,
  12-month residual blocks; every row `is_proxy=True`, rule `PROXY-EQUITY-VOL-HAR-V1`; no observed or VXO-spliced month touched.

## Standing caveats

- Backcast months are MODEL OUTPUT. Behaviour in pre-1986 tails is
  extrapolation, not evidence; the 1986-89 held-out check is the closest
  audit that exists and it is 48 months long.
- Nothing consumes this series. Admitting it to anything is a separate
  amendment with its own evidence bar, and
  `governance/proposed/PROPOSED-AM-block-draw-span-1986.md` argues AGAINST
  admitting model months to the benchmark span at all.

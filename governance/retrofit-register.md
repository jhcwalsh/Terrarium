# Retrofit register

**What this is.** A dated, append-only log of ideas, gaps, and scope items that surface
*during* a work package but are not that work package's job to fix. WP2.1b's own scope
discipline states the rule plainly: "if an idea arrives during this work that is not one
of the [named] items, write it to the retrofit register and move on." This file is where
it lands.

**Rules.**
- **Append-only.** New rows go at the bottom of the table. Never edit, delete, or
  reorder an existing row -- if a deferred item is later resolved, add a new row (or a
  note in the decision register) that references this row's `id`, rather than rewriting
  history here.
- Every row must name where the item actually lands (a work package, a decision-register
  id, or "unscheduled" if genuinely not yet planned) so a reader can trace it forward.
- This file records *that* something was deferred and *why*; it does not carry the
  technical detail of the deferred item itself -- that lives wherever the item is
  properly documented (a plan section, `factors.yaml`, `pre-registration.yaml`, etc.).

| date | id | raised in | item | why deferred | where it lands |
| --- | --- | --- | --- | --- | --- |
| 2026-07-25 | RFR-1 | WP2.1b Task 2 (D4 strategy-set redefinition) | `commodities` has no Step-1 data source. It is declared in `factors.yaml`'s `global` block and carries weight in two D4 strategies (`eqw_factors` 0.20, `endowment_proxy` 0.10 -- see `pre-registration.yaml`'s `rationale.d4_commodities_consequence`), but no series in `requirements.yaml` sources it. | Registering a commodities series is a new connector, which falls under the `requirements.yaml` §WP1.9 emergent-requirements rule -- out of scope for a pre-seal patch that is meant to stay narrow (WP2.1b's own scope discipline). | WP2.2 (register the series; the two affected D4 strategies' reference statistics become computable once it lands) |
| 2026-07-25 | RFR-2 | WP2.1b Task 5 (governance) | FX block (decision R5) -- unhedged foreign-currency exposure is out of scope for v1. | Closed CLOSED-deferred rather than resolved open: adding an `fx` block after the generator is trained requires new cross-block correlation the trained weights don't have. | Re-entry path is a `block_addition` amendment (`governance/amendment-log.yaml`) plus a full generator retrain; see `governance/decision-register.md`'s Step 2 section, row `R5` |
| 2026-07-25 | RFR-3 | WP2.1b Task 5 (governance) | UK factor block (decision J3) -- `bank_rate`, `gilt_nominal_10y`, `gilt_real_10y`, `rpi`, `cpi_uk` (declared but inactive in `factors.yaml`'s `uk` block). | Closed CLOSED-deferred rather than activated: activating it before the seal would have required the Step-1 UK data connectors (BoE curves, ONS RPI/CPI) to exist first, and `Instructions/WP2.1b-PRE-SEAL-PATCH.md` explicitly says not to hold the seal for them. | Re-entry path is a `block_addition` amendment plus a full generator retrain, same as R5; see `governance/decision-register.md`'s Step 2 section, row `J3`. `Instructions/WP1.12-UK-CONNECTORS.md` scopes the connector work that would precede activation. |
| 2026-07-25 | RFR-4 | WP2.1b final branch review (pre-seal patch) | `ah.gen.base.EnsembleMeta.active_blocks` has no producer. The field exists with default `()` and the battery already records it, but no generator exists yet to populate it, and nothing validates a populated value against the manifest. | Building the generator that would set it is WP2.2/WP2.4 scope, not a pre-seal-patch fix; the field and its default are correct as of WP2.1b, only its producer is missing. | WP2.2/WP2.4 -- every generator must set `active_blocks`, and validation against `ah.factors.load_manifest().active_blocks` should reject a mismatch. |
| 2026-07-25 | RFR-5 | WP2.1b final branch review (pre-seal patch) | Threshold-vs-data-availability gap: `ah.eval.prereg.verify()` checks that a threshold key names a real factor in the right block and a registered stat, but not that the factor has data. A threshold such as `"commodities.skew"` at `enforce` severity would seal cleanly while `ah.eval.reference.compute_reference` records `commodities` in `missing_factors` and computes nothing for it (see RFR-1). | Closing this requires `reference.py`'s `missing_factors` output as an input to `verify()`, which is WP2.2/WP2.3 scope; this patch's Fix 3 only aligns `_check_conventions` with `ah.strategies`, a distinct hole. | WP2.3 -- before sealing, cross-check every threshold key against the reference run's `missing_factors` and reject any `enforce` (or any) threshold keyed to a factor with no computed reference statistic. |
| 2026-07-25 | RFR-6 | WP2.1b final branch review (pre-seal patch) | `ah/splits.py`'s docstring promises the train/validation/holdout dates "are sealed in pre-registration.yaml (WP2.3)", but `pre-registration.yaml` has no `splits:` section yet. `splits.py` is now itself a hashed judged source (this patch's Fix 1), so the code's dates and the not-yet-added sealed dates cannot silently diverge once WP2.3 seals -- but they are not yet reconciled either. | Adding the sealed splits section is WP2.3's own sealing act, not a pre-seal-patch documentation fix. | WP2.3 -- add a `splits:` section to `pre-registration.yaml` recording the TRAIN/VALIDATION/HOLDOUT boundaries, and a test asserting `ah.splits.SPLITS` matches it exactly before sealing. |
| 2026-07-25 | RFR-7 | WP2.2 Task 1 review fix pass (Critical 2) | `fred.FEDFUNDS` (effective federal funds rate) registered in `requirements.yaml` under the §WP1.9 emergent-requirements rule, with a `fedfunds_pre1954` proxy rule in `ah.data.splice` backfilling pre-1954-07 history from `fred.TB3MS` (flagged `is_proxy`), and an offline connector fixture. NOT deferred -- **resolved in the same commit**; this row exists because RFR-1 assigned emergent-series registration to WP2.2 and the register is the trace. | n/a -- resolved. `policy_rate` mapped to a market bill yield would have shared its construction with `funding_spread`'s TED leg and sealed the cross-block correlation bands over an artifact of the mapping. | Closed in WP2.2 Task 1's fix pass. Live fetch is still untested by construction (no network in CI); the splice rule is registered but not yet applied by `ah.data.refresh`, exactly as `hy_oas_pre1996` is not -- see RFR-10. |
| 2026-07-25 | RFR-8 | WP2.2 Task 1 review fix pass (Critical 2, following RFR-1) | `commodities` still has no Step-1 data source. RFR-1 assigned this to "WP2.2 (register the series)" and WP2.2 Task 1 then cited RFR-1 as the *reason* `commodities` is unavailable -- circular. This row breaks the circle by re-deferring it explicitly, with a new owner and a real reason. | Unlike `fred.FEDFUNDS` (RFR-7), no free, already-connectored commodities total-return series exists: the honest candidates (Bloomberg/S&P GSCI, BCOM) are licensed (`license_tier: COMM`), so registering one is a licensing decision plus a new manual-intake schema, not a one-line manifest entry. WP2.2 Task 1's scope was the factor->series mapping; inventing a proxy (e.g. a producer-price index) for a factor that carries 0.20 of `eqw_factors` and 0.10 of `endowment_proxy` would be exactly the substitution this project refuses to make silently. | **WP2.3**, as an explicit pre-seal decision, not a drive-by: either (a) license and register a commodities series and recompute the two affected D4 strategies' bands, or (b) seal `eqw_factors`/`endowment_proxy` with `commodities` absent and record the consequence as a dated, named limitation in `G2-EVIDENCE.md`. `pre-registration.yaml`'s `rationale.d4_commodities_consequence` already states what (b) costs. `factor_sources.commodities` now carries `numeraire: total_return` so the assumption D4 makes about it is machine-visible. |
| 2026-07-25 | RFR-9 | WP2.2 Task 1 review fix pass (Critical 3) | `credit_xs_hy` is declared `numeraire: zero_cost` -- a duration-hedged long-HY/short-government position, self-financing and therefore numeraire-neutral. That is economically defensible, but `endowment_proxy` weights it at 0.15 alongside capital-committing total-return legs whose weights sum with it to 1.0, so the 0.15 is a notional/risk budget for that leg and a capital share for the others. | Making the credit sleeve a genuine total return would mean adding a duration-matched government leg at HY spread duration (4.0y) rather than the sealed `govt_tr_10y` duration (8.5y) -- a new derived series and a change to a sealed D4 strategy definition, which is well beyond a review fix pass and belongs with whoever authors the real bands. | **WP2.3** -- decide whether `endowment_proxy`'s weights are capital shares throughout (add the duration-matched government leg) or explicitly a risk budget (say so in the sealed `notes`). Either way it is a stated decision before the seal, not an inherited ambiguity. The loader now rejects an *undeclared* mixed leg, so the exception cannot grow silently. |
| 2026-07-25 | RFR-10 | WP2.2 Task 1 review fix pass | Two Step-1 modules now materially determine sealed band values but are NOT in `ah.eval.prereg._REQUIRED_JUDGED_SOURCES`: `src/ah/data/derive.py` (every `kind: derived` factor's transform -- `add` for `equity_mkt`, `difference` for `ig_spread`, `funding_stress` for `funding_spread` -- plus `assemble_panel`) and `src/ah/data/splice.py` (the `PROXY_RULES` that backfill `policy_rate` and `hy_spread`). An edit to either changes what a sealed band is a band *of*, with no lock violation. | Adding them to the seal would make every future Step-1 change a dated amendment, which is a governance decision with real cost, not a review fix. `ah.eval.panel`'s `_DERIVED_EXPRS` table (which *is* sealed) pins which helper each factor uses and its units algebra, so the binding is sealed even though the helper body is not. | **WP2.3** -- decide explicitly, alongside the existing `battery/thresholds.yaml` question (see the seal-scope note in `pre-registration.yaml`'s header): seal `derive.py`/`splice.py`, or state that the sealed `_DERIVED_EXPRS` binding plus the sealed formulas in `pre-registration.yaml` are sufficient and record why. Related: neither proxy rule is applied by `ah.data.refresh` today, so the backfills exist as registered rules rather than as catalog data -- WP2.3 must confirm which the campaign vintage actually contains. |
| 2026-07-25 | RFR-11 | WP2.2 Task 1 review fix pass (discovered incidentally) | `.gitignore`'s `data/` pattern was unanchored, so it matched any directory named `data` at any depth: the entire `src/ah/data/` package (all of Step 1's data layer), every synthetic connector fixture under `tests/fixtures/data/`, and `docs/data/` were untracked. A fresh clone could not run the test suite, and the `v0.2.0` Step-1 tag does not contain the code it tags. NOT deferred -- **fixed in the same commit** (anchored to `/data/`, 54 files added). | n/a -- it blocked this task's own requirement that the new `fred.FEDFUNDS` fixture ship in the same commit. | Closed here. Worth a follow-up: `ruff` respects `.gitignore` by default, so those 34 source files had never been linted -- 13 pre-existing lint findings surfaced and were fixed in this commit. Whoever owns release engineering should check whether the `v0.1.0-g0`/`v0.2.0` tags need re-cutting. |
| 2026-07-25 | RFR-12 | WP2.2 Task 1 review fix pass 2 (widens RFR-9) | RFR-9 named only `credit_xs_hy`'s zero-cost weight in `endowment_proxy` as a mixed capital-share/notional sum. The defect is wider than that framing: the `zero_cost` leg taxonomy is sound and enforced at the *leg* level, but the sealed claim ("every D4 strategy's return series is on the total-return numeraire") is a *portfolio*-level claim, and under `conventions.numeraire: total_return` with `static_weights_composition = sum_i w_i * series_i_t` and no cash leg, any strategy carrying uncommitted capital realizes ZERO on that capital instead of the cash rate -- so it is not only the weights that are mixed, the *realized return series* itself sits below the numeraire by the uncommitted-capital cash return. Three of five D4 strategies are affected, not one: `eqw_factors` commits 0.4 (equity_mkt, commodities) and carries 0.6 of zero-cost notional (smb, hml, mom) that earns nothing; `endowment_proxy` has the same hole at its 0.15 `credit_xs_hy` weight (RFR-9's original finding); and `momentum` -- broken by the WP2.2 Task 1 numeraire switch and not previously recorded anywhere -- realizes exactly `0.0` in its 12-month warm-up and every flat-signal month, which was the economically correct value under the prior Mkt-RF/excess-return numeraire (a cash position has zero *excess* return) but is wrong by the cash rate under `total_return`. `_validate_numeraires` (`ah/strategies.py`) cannot see any of this: it checks each leg's *declared* numeraire, not a strategy's implied uncommitted-capital cash position. | Fixing it means either adding an explicit `cash_tr_1m` residual leg to `eqw_factors`, `endowment_proxy` and `momentum` so uncommitted/flat capital earns cash, or accepting the bias as a sealed, stated limitation -- either way a change to a sealed D4 strategy definition (or an explicit decision not to make one), which belongs with whoever authors the real bands, not a review fix pass. | **WP2.3**, before sealing, must choose between (a) add the `cash_tr_1m` residual leg to all three affected strategies, or (b) seal as-is and record the bias as a dated, named limitation (as RFR-8 does for `commodities`). Do not choose silently -- state which, and why, in `G2-EVIDENCE.md`. Also carried forward here: `ah.strategies`' `static_weights` loader check that a strategy's `weights` sum to 1.0 within 1e-9 now sums mixed capital shares (equity_mkt, govt_tr_10y, commodities, credit_xs_hy in `endowment_proxy`; equity_mkt, commodities in `eqw_factors`) and zero-cost notionals (smb, hml, mom, credit_xs_hy) to reach that total, so "weights sum to 1.0" no longer means "the portfolio is fully invested with no residual cash" for either strategy -- WP2.3 should not treat that check as evidence of full investment when deciding (a) vs (b) above. |
| 2026-07-25 | RFR-13 | WP2.2 Task 2 review fix pass (Critical 1) | The validation battery has an orchestration entry point (`ah.eval.battery.run_full_battery`: compute the train+validation reference, register every reference-dependent suite against it, run) but **no caller** -- no CLI command, no G2 harness, and no negative-control runner invokes it. Related and larger: `_REFERENCE_DEPENDENT_SUITE_BUILDERS` names one suite, `monthly`, because it is the only one that exists, so a real run today produces a MONTHLY-TIER-ONLY verdict whose `passed` covers one of eight suites. Before this fix pass there was no entry point at all and `battery.SUITES` was empty in every production path, so every run returned a vacuously passing, metric-free report -- that half is CLOSED here. | Writing the remaining seven suites is WP2.2 Tasks 3-6's own scope, and the CLI/G2 wiring belongs with whoever first needs to run the battery for real (WP2.4's bootstrap benchmark is the first generator there is anything to run it on). A review fix pass should not invent a CLI surface WP2.3 will seal. | **WP2.2 Tasks 3-6** add each suite to `_REFERENCE_DEPENDENT_SUITE_BUILDERS` in the same PR that writes it (there is a `TODO(WP2.2 Tasks 3-6)` at `battery.SUITES`' declaration). **WP2.4** adds the caller. **WP2.3 must not read a partial battery's verdict as a full one**: until every suite is registered, `BatteryReport.passed` means "no enforce failure among the suites that exist". |
| 2026-07-25 | RFR-14 | WP2.2 Task 2 review fix pass (Minor) | `ah.eval.reference` has no WITHIN-BLOCK pairwise correlation statistic. `CROSS_BLOCK_STATS.correlation` is computed only for factor pairs drawn from two different blocks, so nothing measures (say) `equity_mkt~hy_spread`, both in `global`. The consequence is visible in the one metric that needs a correlation matrix: `cross_block_corr_matrix_distance` can only be a distance to a matrix the reference actually computes, so it covers cross-block pairs and carries that scope in its name (it was `corr_matrix_distance`, which overstated coverage in every report table and threshold key). | Adding a within-block pairwise statistic means a new registry (or widening `CROSS_BLOCK_STATS`' concept beyond its name) plus the per-pair alignment `compute_reference` deliberately scopes per statistic -- a structural change to the reference layer, not a rename, and one that changes what WP2.3 seals. | **WP2.3**, alongside the other band-authoring decisions: either register a within-block pairwise correlation and widen the matrix metric to the full active factor set, or seal the cross-block-only scope explicitly and record that within-block correlation structure is unjudged. Note the second option leaves a real gap -- a generator could get every cross-block correlation right and every within-block one wrong. |
| 2026-07-25 | RFR-15 | WP2.2 Task 2 review fix pass (Important 3, residual) | Reference bootstrap replicates are now length-matched to the judged ensemble's PATH length (`compute_reference(..., resample_length=ensemble.months)`), which makes every per-path statistic's band comparable to the generated side under the n-denominator ACF estimator. It is the wrong length for the POOLED statistics: `ah.eval.metrics.monthly` computes `skew`, `excess_kurtosis`, the Hill indices, the aggregational-Gaussianity kurtoses and `crisis_corr_lift` over `n_paths * months` pooled observations while a replicate carries only `months`, and sample skewness, kurtosis and the Hill index are all small-sample biased -- so for those statistics the two sides match in neither sample size nor bias. Bands are conservatively wide and a fat-tailed generator reads as further from history than it is. | Closing it needs a PER-STATISTIC resample length (pooled statistics matched to `n_paths * months` -- the moving-block draw already supports a replicate longer than the sample; per-path statistics to `months`), which means grouping a factor's stats by required length and drawing indices per group inside `_block_reference`/`_cross_block_reference`. That is a change to the reference layer's draw structure and to what every pooled band is, which belongs with whoever authors the real bands rather than with a review fix pass. | **WP2.3**, before sealing a band over any pooled statistic: either implement per-statistic length matching, or seal the pooled bands with the mismatch recorded as a named, dated limitation in `G2-EVIDENCE.md` (as RFR-8 does for `commodities`). `pre-registration.yaml`'s `conventions.estimator_length_matching` states the residual in the sealed file so it cannot be sealed unknowingly. |
| 2026-07-25 | RFR-16 | WP2.2 Task 2 review fix pass 2 (Minor 4, residual) | `ah.eval.battery.run_battery(ensemble, reference=X, ...)` uses `X` for `_lookup_band`'s band lookup, but the registered `MetricSpec`s (e.g. `cross_block_corr_matrix_distance`) judge against whatever `ReferenceStats` object they closed over at registration time. `run_full_battery` keeps the two consistent by construction (it registers against the same `reference` it computes and passes to `run_battery`), and `register_reference_dependent_suites` is idempotent by replacement (tested: two calls with different references produce different metric values, not the first call's stale ones) -- but a caller of the lower-level `run_battery` directly, with a `reference=` that does not match what was actually registered, gets no error and no warning: bands are looked up against the caller's `reference`, values are computed against whatever the specs actually closed over, and nothing detects the mismatch. | Detecting it means either giving `ReferenceStats` (or the registration call) an identity/digest that `MetricSpec` or `SUITES` carries alongside each spec, and having `run_battery` compare that identity against its own `reference` argument -- a structural change to `MetricSpec`/`register_suite`'s signature, not a review-fix-pass-sized addition, and every existing direct `register_suite(...)` call site in the test suite would need updating. | **WP2.3**, before sealing: either add the reference-identity guard, or state in `G2-EVIDENCE.md` that every production call path goes through `run_full_battery` (which cannot desync) and a direct `run_battery` call with a mismatched `reference=` is a caller error the seal does not defend against. |
| 2026-07-25 | RFR-17 | WP2.2 Task 3 (horizon-tier metrics) | `regime_duration_{mean,p50,p90}` is honestly NaN on every ensemble today. `ah.data.derive.label_regime` classifies a month from five inputs (`usrec`, `cpi_yoy`, `growth_yoy`, `drawdown`, `hy_oas`); three are constructible from generated factors, but `usrec` (NBER recession indicator) and `growth_yoy` (industrial-production growth) are registered Step-1 catalog series (`fred.USREC`, `fred.INDPRO`) with NO factor mapping in `factors.yaml` at all -- DN-1.1 Sec.II.2's Layer-1 "climate" state variables, not yet exposed as Step-2 generator-visible factors. | Adding `usrec`/`growth_yoy` as factors (or building a reduced ruleset over generated-factors-only, as WP2.1b Task 2 did for the D4 strategy set) is a manifest/generator-layer decision, not a metrics-suite review fix; `ah.eval.reference.compute_reference`'s per-statistic alignment also deliberately does not build the multi-source join a real historical band for this stat would need. | **WP2.3 or WP2.5+** -- either add the missing factors (or a Layer-1 climate-state read) to `factors.yaml` and wire a real regime-duration computation, or seal the three names with NaN accepted as a stated, dated limitation (as RFR-8 does for `commodities`). `ah.eval.metrics.horizon.REGIME_RULESET_VERSION` records which ruleset version would apply once the gap closes. |
| 2026-07-25 | RFR-18 | WP2.2 Task 3 (horizon-tier metrics) | `ten_year_return_vs_valuation_{slope,r2}` is honestly NaN on every ensemble today. DN-1.1's design calls for a starting-valuation input (`cape_v` / demeaned log CAPE, `ah.data.derive.demeaned_log_cape`) -- the raw `shiller.cape` series IS registered in `requirements.yaml`, but no factor in `factors.yaml` maps to it. Same Layer-1-climate-state gap as RFR-17, for the valuation state `v_t`. | Same reasoning as RFR-17: adding a valuation factor (and the reference-side joint alignment a real historical band needs) is a manifest/generator-layer and reference-layer decision beyond one metrics-suite task's scope. `ah.eval.reference.valuation_regression` is the tested, ready-to-wire OLS estimator; only the data source is missing. | **WP2.3 or WP2.5+** -- add a `cape_v`/valuation factor to `factors.yaml` and wire the regression against it, or seal the two names with NaN accepted as a stated, dated limitation. |
| 2026-07-25 | RFR-19 | WP2.2 Task 3 (horizon-tier metrics) | `ah.eval.reference.block_bootstrap_band`'s percentile step (`np.percentile` over `n_resamples` resample values) is not NaN-robust: a statistic that can be legitimately undefined on a short, length-matched resample (e.g. `drawdown_depth_duration_rank_corr`, which needs >= 2 pooled drawdown episodes -- a length-24 resample sometimes has fewer) returns NaN for that one resample, and `np.percentile` propagates that single NaN into BOTH `lo` and `hi`, even though `point` (computed on the full, long historical sample) is perfectly well-defined. Discovered via `tests/test_reference.py::test_band_brackets_point_estimate_for_every_stat`, which now documents and skips the case rather than asserting a bracket property that cannot hold for a NaN band. | `block_bootstrap_band` is shared, sealed infrastructure (`ah.eval.reference.py` is itself a `_REQUIRED_JUDGED_SOURCES` entry) used by every registered statistic; switching to `np.nanpercentile` (or a documented minimum-valid-resamples floor) is a change to that shared mechanism's semantics for every existing and future stat, not a fix scoped to one new metric. | **WP2.3** -- decide whether `block_bootstrap_band` should use a NaN-robust percentile (and what that implies for a statistic where MOST resamples are degenerate -- is a band computed from, say, 3 of 1000 non-NaN resamples still meaningful?), or accept and document that a statistic prone to per-resample NaNs simply has no usable band and must be sealed `report`-only or with an explicit minimum-valid-resamples check. |
| 2026-07-25 | RFR-20 | WP2.2 Task 3 fix pass 1 (Critical 2) | `ergodicity_gap` is registered but **structurally unavailable**. DN-1.1 Sec.II.6's 10yr row defines it as "ergodicity (**long-path vs ensemble** stats)"; `ah.eval.reference.ergodicity_gap(long_path, ensemble_paths)` now implements exactly that (and is tested against an ergodic AR(1), an iid process, and a genuinely non-ergodic regime mixture), but `ah.eval.battery.run_battery` is handed one `Ensemble` of production-length paths and no long path, and no generator exists yet to ask for one. The metric is therefore NaN on every ensemble and carries `MetricSpec.status="structurally_unavailable"`. This row also records what it REPLACED: the first implementation took both sides from the same 120-month ensemble, which made it algebraically `\|variance_ratio_120m - 1\|` at production path length -- two sealed names for one number, the duplication this file already refused when it dropped `agg_gaussianity` horizon 1 for being `excess_kurtosis` -- and rested on an iid-within-path null under which a perfectly ergodic stationary AR(1) with phi=0.9 reads as a gap of ~18, i.e. it would have failed a CORRECT generator of every persistent factor (`ust_10y`, `cpi`, `hy_spread`). | Supplying a long path means either a new `Generator` call shape (generate one path of many decades) or a second `Ensemble` argument threaded through `run_battery`/`run_full_battery` -- a generator-layer and battery-API change, not a metrics-suite fix, and there is no generator to exercise it against until WP2.4. | **WP2.4 or later** -- the first generator that can emit a single long path wires it (the estimator is already built and tested); until then **WP2.3** seals the name with NaN accepted as a stated, dated limitation, exactly as RFR-17/RFR-18 do. Note `variance_ratio_120m` remains registered and computable on the ensemble side, so the underlying serial-dependence question is not unmeasured -- see the note in `pre-registration.yaml`'s `conventions.variance_ratio_estimator` about its own historical band needing 100 years of data. |
| 2026-07-25 | RFR-21 | WP2.2 Task 3 fix pass 1 (Important 5) | `lost_decade_frequency` is **NOMINAL**, not real (CPI-deflated). "Lost decade" is classically a real-return concept. `reference.py` previously asserted this choice was "recorded as a retrofit-register row (see `ah.eval.metrics.horizon`'s module docstring)" -- no such row existed and that docstring never mentioned one: a materially weakened definition inside a to-be-sealed file citing a governance record that did not exist. This row is that record. | The statistic is registered in `SINGLE_FACTOR_STATS`, whose reference-side `fn: Callable[[np.ndarray], float]` is applied to exactly ONE factor's own column, so no CPI series is reachable from it at all. A deflated version needs a genuinely joint equity+CPI reference computation, which `ah.eval.reference`'s own docstring flags as future work needing its own per-statistic alignment rather than a reintroduced all-factors join. | **WP2.3** -- either seal the nominal definition as a stated limitation (it is now written out in full in `pre-registration.yaml`'s `conventions.lost_decade_frequency_estimator`), or build the joint deflated statistic first. Nominal is the more permissive definition in an inflationary era: a decade with positive nominal and negative real return does not count as lost. |
| 2026-07-25 | RFR-22 | WP2.2 Task 3 fix pass 1 (review judgment on RFR-17/RFR-18) | §WP1.9's emergent-requirements rule (the route that resolved RFR-7 by registering `fred.FEDFUNDS`) was considered for RFR-17 and RFR-18 and **does not apply**. All three series involved -- `fred.USREC`, `fred.INDPRO` and `shiller.cape` -- are ALREADY registered in `requirements.yaml` with working connectors and fixtures; nothing needs to be sourced, licensed or connectored. The blocker in both rows is purely the `factors.yaml` FACTOR MAPPING: these are DN-1.1 Sec.II.2's Layer-1 climate state variables, and exposing them as generator-visible output factors is a generator-namespace decision (what a generator must emit) rather than a data-layer one. A reader who knows the FEDFUNDS precedent would otherwise reasonably ask why the same route was not taken. | n/a -- this row records a considered-and-rejected route for two existing rows, so neither is later reopened on the false premise that it was a data-availability gap. | Ownership is unchanged: **WP2.3 or WP2.5+**, per RFR-17 and RFR-18. |
| 2026-07-25 | RFR-23 | WP2.2 Task 4 (premise corrected in its fix pass) | The Kupiec/Christoffersen backtests and `elicitability_score` run at the 95% level only, never 99% (`var_95`/`es_95`/`var_99`/`es_99` remain descriptive at both levels). **The premise originally recorded here was wrong and is corrected in place rather than left standing:** the ~1.2 expected exceedances at alpha=0.01 over 120 months is the **per-path** count, but `ah.eval.metrics.tails._backtest_metric` **pools across paths**, so a 1000-path ensemble has ~1,200 expected exceedances at 99% -- ample power for Kupiec, which is a pooled proportion test. The scope decision still stands, on the **Christoffersen** ground: transition counts are per-path by construction (a transition must never cross a path boundary), and at ~1.2 exceedances per path the one-step transition matrix of any single path is essentially empty however many paths are pooled, so the independence and conditional-coverage tests have no power at 99% at any ensemble size. Running the Kupiec and elicitability names at 99% while the four Christoffersen names could not follow would split one sealed backtest family across two levels. | Closing the Christoffersen half needs materially longer judged paths (multi-decade), not a code fix. Closing only the Kupiec/elicitability half is a code change available today, at the cost of a split-level family. | **WP2.3 or later** -- decide explicitly between (a) keeping the family at 95%, (b) adding 99% Kupiec and elicitability names only and stating the asymmetry, or (c) waiting for longer paths (roughly 10x today's 120-month horizon) and adding all six. |
| 2026-07-25 | RFR-24 | WP2.2 Task 4 fix pass 2 (BLOCKING 1) | `pre-registration.yaml`'s `conventions.backtest_reference_sample_size` reports a chi-square TAIL of a one-path-normalized LR (`kupiec_pof_chi2_tail_1path` etc., renamed from `..._pvalue` in this same fix pass), and explicitly disclaims the significance-level reading. A more honest alternative exists and was considered but not built: a **coverage-band formulation**, `kupiec_exceedance_rate` = the observed `p_hat` judged directly against a band (e.g. derived from the binomial sampling distribution at the sealed reference length), rather than routed through a chi-square LR and its tail at all. This would change what the name *means*, not merely rescale it — the same reason `backtest_reference_sample_size` gives for rejecting a coverage-band rewrite mid-fix-pass. | Building it is a redefinition of a sealed-file statistic family, not a fix-pass-sized change, and belongs with whoever authors the real WP2.3 bands, who can weigh it against the chi-square-tail formulation actually shipped. | **WP2.3** — when deriving real thresholds for the six backtest names (see RFR-25), consider `kupiec_exceedance_rate` (a coverage-band judgment on `p_hat` directly) as an alternative to `kupiec_pof_chi2_tail_1path`; it is more honest about what a one-path reference sample can support, at the cost of being a new statistic rather than a renamed one. |
| 2026-07-25 | RFR-25 | WP2.2 Task 4 fix pass 2 | Every threshold `pre-registration.yaml` carries today is a placeholder (see the file's own header), but WP2.3 must not treat "provisional" as interchangeable across commits: this fix pass changed what several sealed names **mean**, or what value they report for the identical input, not merely their scale. Six names changed identity via BLOCKING 1's rename (`kupiec_pof_stat`/`_pvalue` etc. → `..._lr_1path`/`..._chi2_tail_1path`) compounded with BLOCKING 2's fix to what `_lr_1path` actually measures (pinned to `BACKTEST_REFERENCE_MONTHS = 120`, not the judged ensemble's own path length — see NEW-1). `discriminative_score`'s fix pass 1 changed its class-balance handling (Critical 2), and its noise floor is now disclosed (MINOR 4) in a way a threshold author must read before setting a `min:`. `predictive_score`/`tstr_degradation` changed **value**, not identity, via `_fit_gd`'s Lipschitz-bounded step and gradient-norm stopping criterion (fix pass 1, Important 7) — the same nominal name now returns a different number on the same input than it did before that fix. | n/a — this is a record, not a deferred technical item; the fix is "don't do the thing this row warns against," which costs nothing to state now and would be expensive to discover after a seal. | **WP2.3** — derive every threshold in `thresholds.strategies`, `thresholds.panel`'s `discriminative_score`/`predictive_score`/`tstr_degradation` rows, and any band resting on the renamed backtest names from **POST-FIX** runs only. A band computed from a run predating this commit (or WP2.2 Task 4 fix pass 1, for the utility-tier names) is a band over a definition or a value that no longer exists. |
| 2026-07-25 | RFR-26 | WP2.2 Task 4 fix pass 2 (extends RFR-15) | RFR-15 records that `ah.eval.reference`'s length-matching machinery matches replicates to the judged ensemble's own path length by default, and that WP2.3 must decide, before sealing a band over a POOLED statistic, between implementing per-statistic length matching or sealing with the mismatch recorded as a named limitation. `tail_dependence_lower`/`tail_dependence_upper` (WP2.2 Task 4) are registered `length_matched=False` and so sit outside that specific remedy, but they carry a **worse**, three-way version of the same shape of gap, not covered by RFR-15's stated options: the GENERATED side pools `n_paths * months` observations (~120,000 at production scale), the HISTORICAL POINT estimate rests on the full train+validation series (~1,100 monthly observations), and the historical BAND's replicates are drawn at the FULL sample length (not path-length-matched, per `tail_dependence_estimator`'s own stated departure) — three different sample sizes meeting in one comparison, none of which RFR-15's two options ("implement per-statistic length matching" or "seal the pooled-vs-path-length mismatch as a limitation") directly resolves, because RFR-15's frame is a two-way (generated-path-length vs replicate-length) mismatch and this is a three-way one with the generated side pooled across paths *and* an unmatched historical estimator on top. | Resolving it needs a WP2.3-level decision about which of the three sample sizes (if any) the estimator should be re-expressed at — analogous to `backtest_reference_sample_size`'s fix for the D4 backtests, but for a bounded-fraction estimator rather than a linear-in-sufficient-statistics LR, which may not need the identical remedy. Not a fix-pass-sized change. | **WP2.3** — before sealing a band on `tail_dependence_lower`/`tail_dependence_upper`, read this row alongside RFR-15: the two-way remedy RFR-15 states does not apply to these two names, which need their own decision about the generated/historical/band sample-size mismatch. |
| 2026-07-25 | RFR-27 | WP2.2 Task 5 (economics.py) | `implied_sharpe_{EXP,SLOW,REC,CRI,STAG,REF}` (DN-1.1 Sec.II.6's "Economic" row, "Implied Sharpe ratios ... by regime") is **structurally unavailable**, always NaN, `status=structurally_unavailable` -- the identical shape of gap RFR-17/RFR-18/RFR-20/RFR-22 already record for `regime_duration_*`/`ten_year_return_vs_valuation_*`/`ergodicity_gap`: `ah.data.derive.label_regime` needs `usrec` (NBER recession) and `growth_yoy` (industrial production), neither of which has a `factors.yaml` mapping, so no generated path can be honestly regime-labelled today. RFR-22 already recorded that §WP1.9's emergent-requirements route does not apply (both series are already registered in `requirements.yaml`; the blocker is the factor-namespace mapping, a generator-namespace decision, not a data-sourcing one) -- that reasoning applies identically here and is not re-litigated. | Same as RFR-17/RFR-18/RFR-20: closing it needs a `factors.yaml` mapping for `usrec`/`growth_yoy` (a generator-namespace decision) before any regime-conditioned metric can be computed at all. | **WP2.3 or WP2.5+** -- when the `usrec`/`growth_yoy` factor mapping lands (closing RFR-17/RFR-18 too), `implied_sharpe_{regime}` becomes computable from the same regime labels `regime_duration_*` would use; until then WP2.3 seals the six names with NaN accepted as a stated, dated limitation. |
| 2026-07-25 | RFR-28 | WP2.2 Task 5 review fix pass (Important 6, extends RFR-27) | `policy_anchor_deviation`'s `TAYLOR_PI_TARGET = 2.0` is NOT from DN-1.1 (DN-1.1 states its own latent `pi*_t` carries "a diffuse prior -- the target can drift", deliberately not a fixed number) -- it substitutes the conventional post-1990s central-bank inflation target from the literature, and the `phi_c*c_t` cycle term is dropped entirely (no generator-visible proxy, no DN-1.1 `phi_c` prior to substitute). Both substitutions are stated honestly in `ah.eval.metrics.economics`'s module docstring, its estimator code, `pre-registration.yaml`'s `conventions.policy_anchor_deviation_estimator`, and the CHANGELOG -- but until this row, they lived in no governance/retrofit-register.md entry: RFR-27 (raised in the same task) covers only `implied_sharpe_*`'s structural-gap NaN, not this metric's stated-but-unregistered substitution, and every other comparable "WP2.3 must decide" item in this file (RFR-8's commodities substitution, RFR-21's nominal-not-real lost_decade_frequency, RFR-25's renamed backtest statistics) has a durable row here, which is the artifact WP2.3 actually reads -- an implementer's fix-pass report is not. | n/a -- this row is the registration itself, not a deferred technical fix; the substitution and its honest disclosure already exist in code and in the sealed file. | **WP2.3**, alongside RFR-27: when sealing `policy_anchor_deviation`'s real threshold, read `TAYLOR_PI_TARGET`'s literature-substitution and the dropped `phi_c*c_t` term as a stated, accepted limitation of this metric's definition (or, if a generator-visible `pi*_t`/`c_t` proxy is ever built, replace the substitution and note the metric's value changes identity, not merely its threshold, exactly as RFR-25 warns for the renamed backtest names). |
| 2026-07-25 | RFR-29 | WP2.2 Task 5 review fix pass (Important 2) | `money_pump_violations` audits only whether a SINGLE zero-cost leg, on its own, never loses money on some path -- DN-1.1's own economic row names the audit as catching a strictly dominating COMBINATION of generated factors, and this implementation runs no search over weighted combinations of two or more zero-cost legs (e.g. a relative-value spread between `smb` and `hml` that dominates even though neither leg alone does). The narrowing was present in the sealed estimator block (`ah.eval.metrics.economics`'s `_money_pump_violations_metric`, `conventions.money_pump_estimator`) but not flagged as a limitation until this fix pass. | Building the full combinatorial audit (a search over leg combinations, or at minimum all pairwise spreads) is a materially larger estimator than a fix-pass-sized change, and the per-leg check is not vacuous -- it still catches the degenerate single-leg case the deliverable test in `tests/test_economics.py` exercises, and for any genuinely stochastic leg the per-path probability of 120 consecutive non-negative months is astronomically small. | **WP2.3**, before relying on `money_pump_violations` as evidence of no-arbitrage compliance: either build the combinatorial (or at least pairwise) search, or accept and record the per-leg scope as the sealed definition's actual, narrower coverage. |
| 2026-07-25 | RFR-30 | WP2.2 Task 5 review fix pass (Important 7) | `money_pump_violations` and `floor_violations` are `enforce`, `max: 0` in `pre-registration.yaml`, and both NaN when the judged ensemble emits none of the factors they audit (no `conventions.numeraire_zero_cost_legs` member for the former; no `RATE_FLOOR_FACTORS`/`SPREAD_FLOOR_FACTORS` member for the latter) -- under THE ONE NaN RULE a NaN enforce metric FAILS, so `ah.eval.battery.run_full_battery`'s `BatteryReport.passed` is `False` on any ensemble omitting those factors, unconditionally. This was previously true but unstated: no `run_full_battery` test asserted a verdict either way, so the consequence for WP2.4 (whose first bootstrap generator must therefore emit at least one factor from each audited set, or G2 hard-fails on an audit the ensemble structurally cannot be checked against) was discoverable only by reading the code. NOT deferred as an open question -- **decided in this fix pass**: THE ONE NaN RULE's existing, uniform judgment applies here with no metric-specific carve-out (an ensemble that omits the audited factors has, by construction, produced less than one that emits them and passes honestly, which is exactly the failure mode these two gates exist to catch), so NaN continues to fail rather than being softened to a skip. `tests/test_eval_battery.py::test_run_full_battery_orchestration_fixture_fails_on_the_money_pump_and_floor_gates` pins the behaviour. | n/a -- resolved, not deferred; this row is the durable record of a decision made, not an open item. | **WP2.4** -- the bootstrap generator's factor set must include at least one of `smb`/`hml`/`mom`/`credit_xs_hy` and at least one rate/spread floor-bearing factor for `run_full_battery` to reach a genuine (not NaN-forced) verdict on these two gates. |
| 2026-07-25 | RFR-31 | WP2.2 Task 6 (conditional.py) | Every conditional metric regenerates a fresh ensemble per authored/swept world inside its own `fn`, and `ah.eval.battery._run_suites` computes `mc_error` unconditionally for every registered metric via 20 disjoint subsamples -- for this suite that means up to 20x the per-world `.sample()` calls a single evaluation already makes (mc_error recomputes the identical value every time here, since `fn` ignores the passed ensemble's own paths -- see the module docstring and `tests/test_conditional.py::test_mc_error_is_well_defined_and_finite_for_a_conditional_metric` -- but the wasted calls still happen). Cheap today (small hand-built test-double generators; no generator registered in production until WP2.4), but once a real Layer-3 generator with real GPU/CPU sampling cost is registered, a single `run_full_battery` call could make up to 16 metrics x 20 subsamples x (2-8 worlds per metric) = thousands of real `.sample()` calls just for this one suite, dominating the battery's wall-clock cost. | Fixing it needs either a per-suite mc_error exemption (a metric whose `fn` is provably independent of the ensemble argument could report `mc_error=0.0` without resampling at all) or a caching layer keyed on `(generator_id, world_doc, seed)` shared across the 20 subsample calls -- either is a real design decision (mc_error's current contract is uniform across every suite; carving out an exemption changes that contract), not a fix-pass-sized change, and is moot until a real generator exists to make the cost real. | **WP2.4 or WP2.8** -- before the first real (non-test-double) generator is registered and `run_full_battery` is run against it in earnest, revisit whether conditional.py's mc_error subsampling should be short-circuited or cached; until then the cost is real only in wasted wall-clock on cheap test doubles. |
| 2026-07-26 | RFR-32 | WP2.2 Task 6 fix pass 1 (Important 8; **resolves RFR-31**) | RFR-31 deferred conditional.py's `mc_error` cost to "WP2.4 or WP2.8" and framed the fix as "a per-suite mc_error exemption or a caching layer -- either is a real design decision". On review that was under-argued, and it named only one of two consequences. COST (RFR-31's own): ~670 `.sample()` calls per full evaluation (16 metrics x 21 recomputations x 2 worlds), almost all recomputing bit-identical values. HONESTY (unnamed by RFR-31, and the more serious one): subsampling paths that no metric in the suite reads yields exactly `mc_error: 0.0` for values carrying real Monte-Carlo uncertainty -- and `mc_error` is precisely the number a WP2.3 threshold author reads to size a band (STEP2-GENERATOR-PLAN Sec.WP2.2, "MC error << band width"), so a confident 0.0 invites a band far tighter than the metric can support. | Not deferred -- **both closed in this fix pass**. A module-local memo keyed on the world DOCUMENT (canonical JSON), `generator_id`, `n_paths` and `seed` is not a contract change: the regenerated ensemble is provably a pure function of exactly those inputs, which is the same premise RFR-31's `mc_error: 0.0` already rested on. `ah.eval.metrics.conditional._regenerate_cached` implements it (cleared on every `build_conditional_suite`, so a cached ensemble never outlives the registry state and reference that produced it; keyed on the document rather than `world_id` because Part B's sweep worlds reuse one id per (type, level) while their target is a function of the reference). `conditional_mc_error` then replaces the path-subsampling estimator with the spread over `CONDITIONAL_MC_ERROR_REPLICATES` = 8 further REGENERATION seeds -- the axis this suite's randomness actually flows along -- reported as `std(replicates, ddof=1)` (no `/sqrt(k)`: each replicate is an independent re-draw of the whole statistic and the reported value is one such draw). Wired through the additive `ah.eval.battery.MetricSpec.mc_error_fn` hook; every other suite keeps the uniform estimator untouched. Net: ~144 `.sample()` calls instead of ~670, now buying a real error bar instead of a tautological zero. | **WP2.4**, for sizing only -- nothing remains open for correctness. `_REGENERATION_CACHE_SIZE = 32` is sized to one (mean, p90) metric pair's working set (2 worlds x 9 seeds = 18 cached `Ensemble`s); a generator emitting far more factors/months should have that reconsidered against memory, and `CONDITIONAL_MC_ERROR_REPLICATES = 8` against real sampling cost. |
| 2026-07-26 | RFR-33 | WP2.2 Task 6 fix pass 1 (Important 5) | `fixtures/worlds/conditional/*.json` -- the eight authored condition-adherence worlds -- were load-bearing but outside the pre-registration seal. `conventions.condition_adherence_*_estimator` defines each statistic as "pooled across every checked-in fixtures/worlds/conditional/*.json world tagged X", while `ah.eval.prereg._default_judged_sources` hashed only `.py` files plus the two YAMLs. Editing `inflation_severe.worldspec.json`'s `average_pct` from 12.0 to 3.0 therefore changed every inflation metric's value with NO lock violation and no amendment, and the estimator was not reconstructible from the sealed set alone. RFR-31 did not cover this. | Not deferred -- **closed in this fix pass**, by adding the directory to the seal via a new `_REQUIRED_JUDGED_FIXTURE_GLOBS` (REQUIRED, like `_REQUIRED_JUDGED_SOURCES`: an empty glob raises rather than silently shrinking the seal). These are sealed INPUT DATA in exactly the sense `factors.yaml` is -- a document the thresholds are keyed to, not code -- so the alternative (recording a retrofit row and sealing without them) would have left a sealed estimator defined over unsealed inputs, which is the failure mode the seal exists to prevent. `tests/test_conditional.py::test_the_seal_digest_changes_when_an_authored_conditional_world_changes` is the mechanical proof. | **WP2.3** -- consequence to be aware of at seal time, not an open fix: after sealing, editing any authored conditional world requires a dated amendment in `governance/amendment-log.yaml`, exactly as editing `reference.py` does. WP2.3 should also decide whether any OTHER checked-in document a sealed estimator names by directory (there are none today) belongs in the same glob list. |
| 2026-07-26 | RFR-34 | WP2.2b Task 7 review fix pass (evidence-integrity findings) | No metric suite in the battery emits a `<factor>.mean`, `<factor>.std`, or `<a>~<b>.correlation` metric at all, although `ah.eval.reference.SINGLE_FACTOR_STATS`/`CROSS_BLOCK_STATS` register all three and `compute_reference` computes a real, length-matched band for each. `nc3-shifted-bootstrap` (WP2.2b's mean/vol-shifted negative control) drifts on exactly this axis and is invisible to it -- see `ah.eval.negative_controls`'s module docstring, Finding 1, and the paired NC3-vs-NC5 comparison in `tests/test_negative_controls.py::test_finding_the_monthly_tier_cannot_separate_nc3_from_the_undistorted_bootstrap` (rewritten this fix pass to an exact, same-seed set-equality assertion rather than a noise-sensitive `<=` on two independently-seeded ensembles). | Adding `mean`/`std`/`correlation` `MetricSpec`s to a suite (most naturally `monthly`) changes what the monthly tier judges -- a new-band-authoring decision this review fix pass must not make unilaterally (no threshold may be tuned or added to make a designated tier fire). | **WP2.3**, when it authors real bands, alongside RFR-14 (within-block correlation) and RFR-15 (band length-matching): add the three missing `MetricSpec`s so the bands `compute_reference` already computes become judged, not merely computed and reported. |
| 2026-07-26 | RFR-35 | WP2.2b Task 7 review fix pass (Important 7, extends Finding 2) | The battery's `enforce`-severity surface is blind to all five negative controls except one shared, non-discriminating gate (`floor_violations`); every substantive catch in the suite came from a reference band the battery does not judge against or from a `report`-severity threshold. This fix pass makes the gap machine-checkable: `NegativeControlReport`/`ControlOutcome` gained `caught_at_criterion` (and the markdown table's `caught` column was renamed `caught_on_any_surface` with `caught_at_criterion` added alongside it), which is `False` for `nc1-iid-gaussian`, `nc2-shuffled`, `nc4-memorizer`, `nc5-condition-ignoring` -- every control whose `Designation.criterion` is `"enforce"` -- confirming none of them is actually caught at the surface named. | Closing it means either promoting real thresholds to `enforce` severity or making the reference bands themselves gate the battery -- WP2.3's own band-authoring and sealing work, not a review fix pass. | **WP2.3** -- when authoring real thresholds, treat `caught_at_criterion=False` on any control whose designated criterion is `enforce` as unresolved until a real `enforce`-severity threshold exists for that tier. |
| 2026-07-26 | RFR-36 | WP2.2b Task 7 review fix pass (Important 5) | `near_duplicate_fraction`'s real defect is larger than the ordering comparison in Finding 3 (`ah.eval.negative_controls`'s module docstring) reveals on its own: the metric is dominated by whether the GENERATED side's block boundaries align with the memorization suite's own fixed, index-0-anchored 24-month chopping, not by how much of the source is actually reproduced. Measured by varying only `nc4-memorizer`'s start-index distribution (`tests/test_negative_controls.py::test_finding_near_duplicate_fraction_is_dominated_by_block_phase_not_copying`): as-built 0.0654; start snapped to the 24-month grid 0.8875 (an order of magnitude higher from a phase change alone); a ZERO-noise literal verbatim copy at a random (non-grid) offset 0.2423 -- statistically indistinguishable from the plain block bootstrap's own 0.2394, even though a verbatim copy is as memorized as a block can be. Consequence: a *relative* bound against a block-bootstrap baseline (the natural-seeming remedy) inherits the same fixed-grid blindness and would not separate a literal copier from a resampler either. | The real fix -- sliding-window (all-offsets) block comparison on at least one side of `ah.eval.metrics.memorization`'s chopping -- is a change to sealed, judged infrastructure (`_REQUIRED_JUDGED_SOURCES`) and a new statistic, not a review-fix-pass-sized change; `ah.eval.metrics.memorization` is explicitly untouched by this WP. | **WP2.3 or later** -- before relying on `near_duplicate_fraction` (or any bound derived from it) as memorization evidence for a real generator, either move to sliding-window block comparison or record the phase-blindness as a stated, accepted limitation. |
| 2026-07-26 | RFR-37 | WP2.2b Task 7 review fix pass (Important 6, corrects Finding 4) | The `10yr` tier is 73% (16 of 22) structurally-unavailable metrics, not the 59% (13 of 22) the sealed text previously stated: 14 `<factor>.ergodicity_gap` (one per active factor, RFR-20) plus `ten_year_return_vs_valuation_{r2,slope}` (RFR-18) all carry `status=structurally_unavailable`, but 3 of the 16 (`commodities.ergodicity_gap` and both `ten_year_return_vs_valuation_*` names) carry `band=None` and so never land in `CellOutcome.band_nan_metrics`/`enforce_nan_failures`, which is where "13" came from if counted off those buckets alone. Also corrected: `regime_duration_{mean,p50,p90}` is registered at the `1_5yr` tier (`ah.eval.metrics.horizon.TIER_1_5YR`), not `10yr`. Both pinned by `tests/test_negative_controls.py::test_finding_the_10yr_tier_is_structurally_unavailable_on_most_of_its_metrics`. | Not a new technical gap -- the underlying items are already tracked (RFR-17, RFR-18, RFR-20, RFR-22). This row exists so a reader of this register finds the corrected 73%/16-of-22 accounting, not the earlier, undercounted 59%/13-of-22 one that had been repeated in `ah.eval.negative_controls`'s module docstring, `CHANGELOG.md`, and a pinned test. | No new owner -- see RFR-17/RFR-18/RFR-20/RFR-22 (WP2.3 or WP2.5+) for the actual gap; this row is the corrected accounting only. |
| 2026-07-26 | RFR-38 | WP2.2b Task 7 review fix pass (Critical 3) | 148 of 3035 finite (value, band) comparisons across the five negative controls sit at EXACTLY zero distance from their own band edge (`value == band.lo` or `== band.hi`), passing today only because a band's bracket is closed (`lo <= value <= hi`); 33 of those 148 rest on a fully degenerate `[0.0, 0.0]` band (7 distinct `tail_dependence_{lower,upper}` metric names whose historical block-bootstrap replicates never once produced a joint exceedance -- e.g. `hy_spread~ust_2y.tail_dependence_upper`). A verdict change on any knife-edge comparison is therefore structurally guaranteed to be visible under an arbitrarily small perturbation, which is sufficient on its own to explain the occasional battery-verdict flip `tests/test_negative_controls.py::test_battery_verdict_is_bit_identical_for_the_same_seed` records -- no BLAS-thread-count hypothesis is needed, and that specific prior hypothesis is now FALSIFIED (the metric-value digest is bit-identical with `OPENBLAS_NUM_THREADS`/`OMP_NUM_THREADS` set to 1, to 8, and left at the process default). Pinned by `test_finding_the_battery_has_knife_edge_band_comparisons`. | Fixing a knife-edge band (widening it, or an open/tolerance-based comparison) is a change to sealed reference-band semantics (`ah.eval.reference.block_bootstrap_band`, itself a judged source) that would affect every band in the battery, not a fix scoped to the negative-control suite -- the same shape of decision RFR-19 already defers. | **WP2.3** -- when authoring real bands/thresholds, be aware a nontrivial fraction of comparisons sit exactly on a boundary today; decide whether `block_bootstrap_band` needs a tie-breaking convention (e.g. an exact-boundary value fails, or degenerate zero-width bands are widened/excluded) before sealing. |
| 2026-07-26 | RFR-39 | WP2.2b Task 7 review fix pass (Critical 2) | The `conditional` suite's SPECIFICITY is untested: all five negative controls ignore `factor_conditions` (none has any conditioning mechanism at all -- see `_Control.sample`'s docstring), and the tier fires substantively for all five, not just `nc5-condition-ignoring` (designated specifically to conditioning). `nc1-iid-gaussian` (whose designated defect is unrelated to conditioning) fires on 12 of NC5's own 14 designated conditional metrics and is ~10x worse than NC5 on `condition_adherence_error_inflation` (220.7 vs 23.3) -- see `ah.eval.negative_controls`'s module docstring, Finding 5, and `tests/test_negative_controls.py::test_finding_the_conditional_tier_fires_for_every_control_not_specifically_nc5`. Whether the tier would PASS a generator that honours conditions while sharing NC5's other properties is unknown; the suite has no such control. | Building a sixth construction that DOES read `factor_conditions` (e.g. a bootstrap that reweights or selects historical blocks toward the requested condition) is a new control -- out of scope for a review fix pass on the existing five, and risks becoming a de facto real generator rather than a control. | **WP2.2b or WP2.4** -- add a condition-honouring negative control (or, at minimum, treat WP2.4's real generator's own conditional-suite results as the first genuine test of the tier's specificity) before treating a `conditional`-suite rejection as diagnostic of conditioning specifically. |
| 2026-07-26 | RFR-40 | WP2.2c Item 2 (**closes RFR-36**) | `near_duplicate_fraction`'s phase blindness is fixed: `ah.eval.metrics.memorization` now searches every generated block against EVERY OFFSET of the TRAIN split (`_sliding_blocks`, stride 1) instead of the index-0-anchored 24-month grid, and the near-duplicate epsilon is recalibrated on the same search (`_sliding_leave_one_out_epsilon`) because a minimum over ~830 candidates is systematically smaller than a minimum over 34. Measured before/after on the WP2.2b fixtures: a literal zero-noise verbatim copy 0.2423 -> ~1.0; the same copy snapped to the grid 0.8875 -> ~1.0 (phase now carries no information); `nc4-memorizer` 0.0654 -> 0.7096. TWO CONSEQUENCES REMAIN OPEN. (a) `nn_distance_p05`/`p50` fall for every generator and their sealed floors (0.05/0.1) are pre-WP2.2c values that no longer mean what they meant. (b) A stationary block bootstrap whose block length reaches the sealed 24-month memorization window emits literal verbatim historical windows and scores ~0.93 (`nc5-condition-ignoring`, blocks of exactly 24) -- correctly, but it therefore FAILS the newly-`enforce` `near_duplicate_fraction` gate. | (a) is a threshold-authoring job that must be done from a post-WP2.2c run, which is WP2.3's own work and cannot be done from the synthetic CI fixture. (b) is a genuine conflict between two sealed choices (the 24-month memorization window and the benchmark's block-length distribution), not a defect of either -- resolving it is a decision about what `bootstrap-v1` IS, which belongs to whoever specifies it. | **WP2.3** for (a) -- derive both `nn_distance_*` floors from a post-WP2.2c run, never from the current placeholders. **WP2.3 + WP2.4** for (b) -- `bootstrap-v1` must use blocks materially shorter than 24 months, or carry a dated exemption in `governance/amendment-log.yaml`; decide before the G2 bake-off, not during it. |
| 2026-07-26 | RFR-41 | WP2.2c Item 4 | `ah.eval.metrics.economics.SPREAD_FLOOR_PCT` is changed from DN-1.1 Sec.II.4's literal 100bp to 0.0, a stated DEVIATION from the normative document's text. Reason: at 100bp the gate rejected the historical record itself (real TED spreads sat between 15 and 40bp for most of 2010-2020), so it fired identically for all five negative controls INCLUDING the three that replay real history verbatim -- WP2.2b's Finding 2, and the reason `BatteryReport.passed` was False for all five for a reason unrelated to any of their defects. DN-1.1's floors are a GENERATIVE device (a softplus offset that keeps the modelled spread process positive); the falsifiable statement that survives as an audit of a generator's output is that a credit or funding spread cannot be negative. At 0.0 the gate is violated by no historical observation and now fires for `nc1-iid-gaussian` ALONE among the five, which is a detection rather than a constant. | Not deferred -- **decided in this work package**, because leaving it would have left the only firing `enforce` gate in the battery incapable of discriminating anything, which is precisely what WP2.2c was directed to fix. Recorded here because it is a departure from a normative document's literal number and must not be discovered later as an unexplained discrepancy. | **WP2.3** to ratify at seal time. The alternative deliberately NOT taken, and which WP2.3 should consider explicitly: deriving the floor per factor from the train+validation panel's own observed minimum, which would convert this from a structural constraint into a data-dependent band -- a different kind of object, and one that could not be checked against a generator emitting a factor history never covered. |
| 2026-07-26 | RFR-42 | WP2.2c Item 5 (**extends RFR-37**) | The `10yr` tier's 73% structural unavailability is now DISCLAIMED IN THE SEALED FILE (`pre-registration.yaml`'s `conventions.ten_year_tier_coverage`, pinned by `tests/test_negative_controls.py::test_the_10yr_tier_unavailability_is_disclaimed_in_the_sealed_file`) rather than left implicit. The disclaimer states the 16-of-22 count, names all three causes (no CAPE/valuation factor; no recession/growth indicator; `ergodicity_gap` needs a multi-century path no generator emits), states that `nc2-shuffled` is DESIGNATED to this tier and produces zero substantive failures in it, and forbids `G2-EVIDENCE.md` from citing a 10yr pass. Nothing was wired: no available factor closes any of the three, and the WP2.2c brief forbids inventing one. | Not deferrable further and not fixable here -- the blockers are missing INPUTS. A valuation factor is a Step-1 data addition (a new `requirements.yaml` series plus a `factors.yaml` mapping), which is a connector work item nobody owns today; the regime-duration inputs arrive with WP2.6; `ergodicity_gap` needs a generator capability (emit one multi-century path) that WP2.5's climate model could supply. | **Unscheduled** for the valuation factor -- it is a Step-1 data item with no current owner and it should be given one before G2, since without it DN-1.1's decade row cannot be posed at all. **WP2.6** for regime duration, **WP2.5** for `ergodicity_gap`. **WP2.3** must seal the tier as UNAVAILABLE rather than as a tier that passed. |
| 2026-07-26 | RFR-43 | WP2.2c Item 3 | `tail_band_exceedance_fraction` is sealed `severity: report`, not `enforce`, and the tail machinery therefore blocks nothing. The gate is a majority rule over a family spanning all 13 active factors, but a tail failure is characteristically confined to the return-bearing ones (4 of 13 today, the rest being levels), so it cannot reach 0.5 however severe it is: `nc1-iid-gaussian`, which replaces every fat tail in the panel with a Gaussian one, scores 0.234 -- while producing 11 substantive tail band failures on exactly the `excess_kurtosis`/`hill_tail_index` statistics DN-1.1 Sec.II.6 names. CONSEQUENCE: a generator that got the dynamics right and the tails wrong passes at `enforce` today. NC1 is blocked, but by the dependence gate, for a different (also real) defect. | Closing it needs either a gate scoped to the return-bearing factors (a second family axis this work package did not introduce, since choosing the scope by which control it catches is exactly the threshold-fitting the WP2.2c brief forbids) or sealed absolute bands on the tail statistics derived from a real run, which is WP2.3's band-authoring work. Lowering the 0.5 bound was rejected: it is derived from the sealed band `level` and a majority rule, and moving it to make a control fire would destroy the only evidence the gate is not fitted. | **WP2.3** -- when authoring real bands, either add a return-bearing-scoped tail gate or seal absolute tail thresholds, and until then do not read a `tail_band_exceedance_fraction` pass as evidence that tail behaviour was checked at a blocking level. |
| 2026-07-26 | RFR-44 | WP2.2c Item 3 | The three `*_band_exceedance_fraction` gates each re-evaluate their family's per-factor metric functions, and `ah.eval.battery._run_suites` computes `mc_error` for every registered metric via up to 20 disjoint path subsamples -- so each gate costs one extra pass over its family per subsample. A single-slot, identity-keyed cache (`ah.eval.metrics.monthly._cached_band_exceedances`, the same construction `memorization._cached_pooled_signals` uses) collapses the three gates into ONE pass per ensemble object, but `mc_error` builds a fresh sub-`Ensemble` per subsample, so the cache misses on every one: the net cost is ~25 extra passes over the ~104 family comparisons per judged ensemble. Measured effect: `tests/test_negative_controls.py` went from roughly 5-6 minutes to ~9, and the full suite from 10+ to 20+. Cheap in absolute terms today (the family statistics are all O(n) per path) but it scales with the family size and with `_DEFAULT_MC_SUBSAMPLES`. | Fixing it properly means a `MetricSpec.mc_error_fn` override for the gates -- exactly the hook RFR-32 added and `ah.eval.metrics.conditional` already uses -- and a decision about what a Monte-Carlo error bar on a fraction-of-exceedances even means (subsampling paths changes the metric VALUES the fraction is computed from, so the subsample estimate is not an estimate of the same quantity at full path count). That is a real estimator decision, not a performance tweak, and it is the number a WP2.3 threshold author reads to size a band. | **WP2.3** -- decide the gates' mc_error estimator (or an explicit exemption) when sealing their bounds; the reported error bar should not be trusted for band-sizing until then. |
| 2026-07-26 | RFR-45 | WP2.2c honesty fix pass (Important 3) | The three `*_band_exceedance_fraction` gates' design (the choice of `acf_r_sum`/`acf_abs_sum` over the 403-per-lag aggregate, and over the two-stage majority variant) was selected by measuring all three against the five negative controls and keeping the one that separates them. `pre-registration.yaml`'s `band_exceedance_gate_estimator` now states this explicitly: for these three gates, the negative controls are NOT independent evidence that the gate design works -- they were the evidence used to choose among candidate designs. Measured counterfactual: including the 403 per-lag comparisons alongside the two sums and the decay rate in one family gives `nc1-iid-gaussian` 0.389, UNCAUGHT at the sealed 0.5 bound. `bootstrap-v1` (WP2.4) is the first genuinely independent test of the design. | n/a -- this row is the tracking record, not a deferred technical fix; the disclosure itself already lives in `pre-registration.yaml`. | **WP2.3**, when `G2-EVIDENCE.md` is first written: draw this paragraph from `pre-registration.yaml`'s `band_exceedance_gate_estimator` convention rather than restating "catches 4 of 5 negative controls" as if it were independent validation of the gate design itself. |
| 2026-07-26 | RFR-46 | WP2.2c honesty fix pass (live option, not implemented) | A baseline-relative memorization bound (judging `near_duplicate_fraction` -- or `nn_distance_p05`/`p50` -- against a matched-block-length resampler's own score, rather than an absolute bound) was rejected by WP2.2b on the ground that it "inherits the phase blindness" of the fixed-grid search. That reasoning is now STALE: WP2.2c Item 2 made the near-duplicate search phase-blind (sliding-window, all-offsets), so a relative bound no longer inherits a phase confound it was rejected for. It is a live, non-fitting option with real power the current absolute bound lacks -- separating a literal copier from a matched-block-length resampler, which today both can score similarly on an absolute scale once block length reaches the memorization window (RFR-40's open item (b)). | Building and validating it is a new statistic (a relative bound, not a rename) and a new threshold-authoring decision, not a fix-pass-sized change -- and it interacts with RFR-40(b)'s open question about `bootstrap-v1`'s own block length. | **WP2.3** -- consider a baseline-relative memorization bound as an alternative or supplement to the absolute `near_duplicate_fraction`/`nn_distance_*` bounds, now that the phase-blindness objection no longer applies; decide alongside RFR-40's two open items. |
| 2026-07-26 | RFR-47 | WP2.2c honesty fix pass (live option, not implemented) | A per-factor combination for the tail gate (e.g. max-over-factors of within-factor exceedance, or a Simes/min-p combination across the 13 active factors) with its bound derived from the sealed band `level` and the family size, would have power against a tail failure concentrated in a few units -- exactly `nc1-iid-gaussian`'s shape (its tail damage concentrates in the 4 return-bearing factors of 13, which is why the plain majority-rule `tail_band_exceedance_fraction` cannot reach 0.5 however severe the failure is; RFR-43). Not implemented here: choosing a gate construction BECAUSE it separates NC1 would repeat exactly the Important-3 problem (RFR-45) this same fix pass just disclosed, so validating this option on NC1 specifically must be avoided or the same caveat recorded again. | Deriving the bound from `level` and family size alone (not from NC1's measured score) is real work belonging with whoever authors WP2.3's real tail thresholds, and the caveat about not using NC1 to choose among variants must travel with whatever is built. | **WP2.3** -- consider a per-factor (max-over-factors or Simes/min-p) tail gate as the return-bearing-scoped alternative RFR-43 already calls for; if any candidate construction is validated against NC1 during that work, record the same Important-3-shaped caveat RFR-45 states, rather than citing the catch as independent evidence the design works. |
| 2026-07-26 | RFR-48 | WP2.3 (the seal) — **closes RFR-5** | Threshold-vs-data-availability, closed with a machine check and one real removal. `ah.eval.prereg.verify()` now reads the sealed `reference_run.missing_factors` / `reference_run.uncomputable_d4_strategies` lists and REJECTS any threshold keyed to a factor or D4 strategy named in them; it runs on every battery invocation. The cross-check found a live defect: `policy_rate.excess_kurtosis` was sealed at **enforce**, and `policy_rate` has no train+validation data on the campaign vintage — under THE ONE NaN RULE that threshold would have failed every run forever. It is removed. The vintage is missing **three** of fourteen declared factors, not one: `commodities` (declared unavailable), `policy_rate` (mapped to `fred.FEDFUNDS`, registered by WP2.2 Task 1 but absent from the 2026-07-24 vintage, which predates it) and `hy_spread` (mapped to `fred.HY_OAS`, whose ~3 licensed years all fall inside the holdout span). Consequence, wider than `rationale.d4_commodities_consequence` stated: **three** of five D4 strategies have no computable historical statistics (`eqw_factors`, `endowment_proxy`, `carry`), leaving `sixty_forty` and `momentum`. | Closed in WP2.3. **WP2.4** should note the corollary: `bootstrap_v1.factor_set` is the eleven factors with data, and only those. Restoring `policy_rate`/`hy_spread` means applying the registered splice rules and refreshing to a new vintage, which is a campaign restart. |
| 2026-07-26 | RFR-49 | WP2.3 (the seal) — **closes RFR-6** | `pre-registration.yaml` now carries a `splits:` block (train 1871-01-01..2011-01-01, validation ..2021-01-01, holdout ..2026-08-01, `start` inclusive / `end` exclusive), and `verify()` checks `ah.splits.SPLITS` against it exactly on every battery invocation, with the sealed document normative. `ah/splits.py` was already hashed, so a boundary change already broke the lock — but a lock violation says "some hashed file changed", not "the reference span moved", and this makes the divergence name itself. | Closed in WP2.3. |
| 2026-07-26 | RFR-50 | WP2.3 (the seal) — **closes RFR-10, and RFR-33's open question** | Seal scope decided explicitly, in three parts. (a) **`src/ah/data/derive.py` is now SEALED**: it is on the read path, not merely referenced by it — `ah.eval.panel._DERIVED_EXPRS` calls `derive.add`, `derive.difference` and `derive.funding_stress` at panel-read time for `equity_mkt`, `ig_spread` and `funding_spread`, so every band for those three factors is a function of those helper bodies, recomputed on each read rather than stored in the vintage. Sealing only the binding via `panel.py` was not enough. (b) **`src/ah/data/splice.py` is NOT sealed**: its `PROXY_RULES` are registered but not applied by `ah.data.refresh`, and `read_factor_frames` never calls it — which the reference run *demonstrates* rather than assumes (`policy_rate` and `hy_spread` land in `missing_factors` precisely because the backfills are absent). RFR-10 also asked WP2.3 to confirm which the campaign vintage actually contains: **neither backfill**. If a later WP applies either rule, that produces a new vintage (a campaign restart and an amendment regardless) and splice.py joins the seal at that point, because it will then be on the read path. (c) **`src/ah/battery/thresholds.yaml` is now SEALED**: every entry is `status: todo` and blocks nothing, but it is read by the already-sealed `ah/battery/report.py`, and a sealed estimator over an unsealed input is exactly the failure RFR-33 closed for the authored conditional worlds. RFR-33 also asked whether any OTHER checked-in document a sealed estimator names by directory belongs in `_REQUIRED_JUDGED_FIXTURE_GLOBS`: audited at seal time, `fixtures/worlds/conditional/*.json` is still the only one. | Closed in WP2.3; recorded verbatim in `pre-registration.yaml`'s `seal_scope:` block and `governance/decision-register.md` row `S2-SEAL-SCOPE-2`. |
| 2026-07-26 | RFR-51 | WP2.3 (the seal) — **closes RFR-12** | The uncommitted-capital numeraire bias: option **(b)**, seal and record, chosen — and **not as a preference**. Option (a), adding a `cash_tr_1m` residual leg to `eqw_factors`, `endowment_proxy` and `momentum`, was found to be *impossible on the sealed vintage*: `cash_tr_1m` derives from `policy_rate`, which has no data (RFR-48), so option (a) would have made all three strategies uncomputable rather than unbiased — it would have propagated the gap instead of closing it. Sealed consequence: each of the three realizes exactly 0.0 on uncommitted or unallocated capital instead of the cash rate, so its realized series sits below the total-return numeraire by that amount for every affected month. Two of the three are already uncomputable for other reasons; **`momentum` is the one live strategy carrying the bias**, and its sealed `var_95`/`es_95`/backtest thresholds are thresholds on a series biased low in its 12-month warm-up and every flat-signal month. RFR-12's warning that `ah.strategies`' "weights sum to 1.0" check is not evidence of full investment was read and honoured. | Closed in WP2.3 (`decisions.S2-NUMERAIRE-BIAS`). **Re-entry**: when `policy_rate` has data, adding the residual leg is a `threshold_change` amendment plus recomputed bands. |
| 2026-07-26 | RFR-52 | WP2.3 (the seal) — **closes RFR-40 (a) and (b)** | (a) `nn_distance_p05`/`p50` re-derived from a post-WP2.2c run on the sealed campaign vintage. The null is history's own self-similarity — the same quantity `near_duplicate_fraction`'s epsilon is defined as — measured as the pooled leave-one-out nearest-neighbour distance of every non-overlapping 24-month train block against every sliding window of the same factor: **p05 = 0.0548, p50 = 2.1660 (n = 339 over 11 factors)**. The sealed floors are half those values (0.0274 / 1.0830), both `report`, because the factor of two is a stated margin and not a computed quantity. A stated composition mismatch travels with them (the historical pool weights factors by train block count; the generated pool weights them equally). (b) `bootstrap-v1` uses **geometric blocks with mean 6 months**, materially shorter than the 24-month memorization window, so no exemption amendment is needed. **RFR-40(b)'s premise needs correcting and this row is that correction**: its "~0.93" was measured on `nc5`, a *fixed*-24-month-block stitch. Geometric block lengths are a different object and the number does not transfer — measured on the sealed vintage, a stationary bootstrap scores `near_duplicate_fraction` 0.064 at mean block 6, 0.187 at 12, 0.284 at 18 and 0.361 at 24, clearing the 0.5 gate at every length tested. The binding upper constraint is not that gate but `nn_distance_p05`, which collapses to exactly 0.0 once the verbatim-window rate `(1 - 1/L)^23` exceeds its own 5th percentile (measured 0.715 at L=6, 0.512 at L=8, 0.073 at L=10, 0.000 at L>=12). The lower constraint is `dependence_band_exceedance_fraction`, which **fails outright at mean block 3 (0.515 against a max of 0.5)**. The window is roughly 4 <= L <= 8 and 6 is its middle. | Closed in WP2.3. **WP2.4** must re-measure the window's edges at the sealed criterion ensemble size (1000 paths); the numbers above come from a 200-path throwaway prototype. |
| 2026-07-26 | RFR-53 | WP2.3 (the seal) — **closes RFR-41** | `SPREAD_FLOOR_PCT = 0.0` RATIFIED at seal time, on evidence measured from the sealed campaign vintage rather than taken on trust: 54.0% of `ig_spread`'s 1224 train+validation observations (minimum 0.32) and 86.9% of `funding_spread`'s 420 (minimum 0.118) sit below DN-1.1 §II.4's literal 100bp; none of either sits below 0.0. `hy_spread`, the third `SPREAD_FLOOR_FACTORS` member, has no data on this vintage and could not be checked either way. The alternative RFR-41 asked WP2.3 to consider explicitly — a per-factor floor derived from each factor's own observed minimum — is recorded as considered and NOT taken, because it converts a structural constraint into a data-dependent band that could not be checked against a generator emitting a factor history never covered. | Closed in WP2.3 (`decisions.S2-SPREAD-FLOOR`). |
| 2026-07-26 | RFR-54 | WP2.3 (the seal) — **records RFR-14/15/16/19/23/24/26/28/29/37/38/42/43/44 as sealed, stated limitations** | Fourteen rows asked WP2.3 either to fix something or to seal it as a named limitation. Every one of them is sealed as a limitation, and every one is written out in `pre-registration.yaml`'s new `limitations:` block so it is inside the hash rather than in a governance file a reader may not open. The consolidated list, by what a reader must not conclude: within-block correlation is **unjudged** (RFR-14); the pooled statistics' bands are conservatively wide because replicate length is matched to the per-path length (RFR-15) and `tail_dependence_*` carries a three-way version of that mismatch (RFR-26); a direct `run_battery` call with a mismatched `reference=` is a caller error the seal does not defend against (RFR-16); `block_bootstrap_band` still uses a NaN-propagating `np.percentile`, with `n_valid_resamples` recording the degeneracy (RFR-19); the backtest family runs at 95% only and the coverage-band reformulation was considered and not adopted (RFR-23/RFR-24); `policy_anchor_deviation` substitutes a literature inflation target and drops the cycle term (RFR-28); `money_pump_violations` audits single legs only (RFR-29); the `10yr` tier is 73% unavailable and **must not be cited as passing** (RFR-37/RFR-42); 148 of 3035 comparisons sit exactly on a band edge and the exact-boundary convention is sealed as is (RFR-38); **the tail gate cannot fire at enforce** and a generator that got the dynamics right and the tails wrong passes today (RFR-43); and the three gates' MC error bars must not be used to size a band (RFR-44). | Sealed, not closed. Each underlying item keeps its original owner. **WP2.4** and later must read `pre-registration.yaml`'s `limitations:` block before citing any threshold as evidence. |
| 2026-07-26 | RFR-55 | WP2.3 (the seal) — **closes the enforcement half of RFR-17/18/20/22/27/37/42** | The twelve `structurally_unavailable` statistic names (`ergodicity_gap`, three `regime_duration_*`, six `implied_sharpe_*`, two `ten_year_return_vs_valuation_*` — 25 metric instances across the active factor set) are now sealed in a `structurally_unavailable_statistics:` block with, for each, the work package that restores it. `ah.eval.prereg.verify()` REJECTS `severity: enforce` on any of them, on every battery invocation: a NaN enforce metric fails every run forever, and before WP2.3 the status was visible on the `MetricSpec` but nothing stopped a threshold being sealed on it. `tests/test_prereg.py` asserts the sealed list equals, in both directions, the set a real suite registration actually marks — the only thing tying the sealed list to reality, since `verify()` cannot import the suites. **Owners**: `ergodicity_gap` → WP2.5 (first generator that can emit a long path); `regime_duration_*` and `implied_sharpe_*` → WP2.6 (regime labels); `ten_year_return_vs_valuation_*` → **unscheduled**, and RFR-42 already says that valuation factor must be given an owner before G2. | Enforcement closed in WP2.3; the underlying gaps keep their owners. |
| 2026-07-26 | RFR-56 | WP2.3 (the seal) — **new finding, discovered while specifying the benchmark** | **A multivariate block bootstrap over every factor with data can only draw blocks from 1990-01 to 2020-12 (372 months).** Multivariate blocks mean one shared row index across all factors, so a block can only be drawn where the whole panel exists — and `equity_vol` (fred.VIX) begins 1990-01, `funding_spread` (TED) 1986-01 and `hqm_curve` 1984-01, while the equity factors reach back to 1926 and `cpi` to 1913. Requiring all eleven simultaneously collapses a 108-year panel to a 31-year one. Two consequences, both sealed in `bootstrap_v1.block_draw_span_consequence`. (1) **`bootstrap-v1` cannot run WP2.11's severe test at all**: "exclude the 1970s" is vacuous for a resampler that has no 1970s, and "regenerate from the 1965 climate state" is impossible. `G2-EVIDENCE.md` must record the benchmark's severe-test row as NOT POSABLE with this reason, rather than as a pass, a fail, or an omission. (2) The benchmark's tail statistics come from a window whose worst equity drawdowns are 2000-02, 2008-09 and 2020, while every reference band it is judged against is computed on each factor's own full history — conservative for a pass, but it means a `bootstrap-v1` pass is weaker evidence than it looks. | **WP2.4 / WP2.11**, and partly unscheduled. Three routes out, none taken here: apply the registered splice rules and refresh to a vintage carrying long `policy_rate`/`hy_spread` history (a campaign restart); drop the short-history factors and resample a longer span with fewer factors; or build a ragged-panel bootstrap (a genuinely new estimator). Any of the three is a dated amendment. |
| 2026-07-26 | RFR-57 | WP2.3 (the seal) — **new finding** | **Three of the four variance-ratio bands are NaN at the sealed replicate length, for every factor.** `ah.eval.reference.VARIANCE_RATIO_MIN_SUMS` is 10 and `nonoverlapping_sums` yields `n // k` sums, so a 120-month replicate (the sealed `reference_run.resample_length`, matched to the sealed ensemble path length) supports `variance_ratio_12m` (10 sums) and nothing longer: 36m gives 3, 60m gives 2, 120m gives 1. Confirmed on the sealed run — `equity_mkt.variance_ratio_60m` has `n_valid_resamples: 0` and NaN bounds. The values are still computed on the generated side and reported; what does not exist is a historical band to judge them against, so `ah.eval.battery.band_is_usable` correctly excludes them from every band comparison and from the three family gates. Consequence: **DN-1.1 §II.6's 1-5yr and 10yr mean-reversion evidence rests on `variance_ratio_12m` alone** at the sealed criterion path length. This is a distinct gap from RFR-20's (`ergodicity_gap` needs a long path *from the generator*); this one needs a longer *replicate*, i.e. a different length-matching convention or a longer judged path. | **Unscheduled**, and it should be given an owner alongside RFR-42's valuation factor. Note the interaction: RFR-15's per-statistic length matching, if ever implemented, would resolve this by drawing these three statistics' replicates at full sample length instead. |
| 2026-07-26 | RFR-58 | WP2.3 (the seal) — **states the sealed enforce surface, and corrects the standing status of RFR-34/35** | Status correction first, because it is easy to get wrong from this register alone: **RFR-34 is already CLOSED** — WP2.2c Item 1 added the `<factor>.mean`, `<factor>.std` and `<a>~<b>.correlation` `MetricSpec`s, so the bands `compute_reference` had been computing since WP2.2 are judged, and RFR-34's own text (written before that landed) reads as still-open only because this register is append-only. **RFR-35 is closed for four of the five controls** by WP2.2c's `moment_`/`dependence_band_exceedance_fraction` gates; the fifth is NC5, whose exemption is now the owner's sealed decision (`decisions.S2-NC5-EXEMPTION`), not a gap. What WP2.3 adds is the statement of the resulting surface: **the sealed enforce surface is exactly five names** — `moment_band_exceedance_fraction`, `dependence_band_exceedance_fraction`, `near_duplicate_fraction`, `money_pump_violations`, `floor_violations` — and nothing else. **Every per-name threshold WP2.3 sealed is `report`**, deliberately, for two stated reasons: at ~570 monthly comparisons a per-name enforce band rejects a perfect generator with probability ~1 (which is why the family gates exist), and the per-name ABSOLUTE bounds WP2.3 did seal (band midpoint ± 4 half-widths) rest on a normal approximation and, for the pooled statistics, on a band drawn at a different sample size than the value it judges (RFR-15) — so a per-name enforce MAX on a pooled tail statistic could reject a CORRECT generator by an amount nobody has measured. | **WP2.4.** `bootstrap-v1`'s run supplies the first empirical observation of the gates' null (`limitations.null_exceedance_rate_is_unverified`), which is what makes per-name enforce bounds derivable; promoting any of them is then a dated `threshold_change` amendment. |
| 2026-07-26 | RFR-59 | WP2.3 (the seal) — **records RFR-46/47 in the sealed file** | Both live options are now written into `pre-registration.yaml`'s `live_options:` block rather than living only here, so a later work package finds them where it will actually look. `baseline_relative_memorization_bound` (RFR-46) is noted as bearing directly on `bootstrap_v1.mean_block_months`: a relative bound would make that choice much less load-bearing than it is today. `per_factor_tail_gate` (RFR-47) carries RFR-47's own trap verbatim — deriving the bound from `level` and family size alone, and re-recording the design-independence caveat if any candidate is validated against nc1. | Unchanged owners; recorded inside the seal. |
| 2026-07-26 | RFR-60 | WP2.3 (the seal) — **new, and the most uncomfortable one** | **The seal's own honesty items are now inside the hash, and one of them limits what WP2.3 itself established.** `pre-registration.yaml`'s `limitations:` block states, in the sealed file: (1) the gates' null exceedance rate is UNVERIFIED — the 0.10-per-comparison premise is the band's definition, not a measurement, and metric correlation breaks the independence the ~3e-8/~1e-10 analytic rates assume, leaving only the Markov bound of 0.2 under arbitrary dependence; (2) the three band-exceedance gates were DESIGNED AGAINST the negative controls (three constructions measured, the separating one kept — the bound never moved, but that limits how bad the practice was rather than denying it), so `G2-EVIDENCE.md` must not cite "catches 4 of 5 controls" as independent validation of the gate design; (3) every negative-control magnitude quoted anywhere comes from a SYNTHETIC fixture at 16 paths, not from the campaign vintage at the sealed criterion size — the structural findings transfer, the magnitudes do not. | **WP2.4** discharges (1) by reporting `bootstrap-v1`'s measured per-comparison exceedance rate whether or not it likes the answer, and is the first independent test for (2). (3) is permanent until a control is rebuilt on catalog data, which nobody owns. |
| 2026-07-26 | RFR-61 | WP2.3 re-seal (branch review, Critical) — **reverses the premise of RFR-48/50/51** | **The campaign vintage was missing `policy_rate` for no reason other than a stale snapshot, and the seal froze that.** `fred.FEDFUNDS` is registered, free, connectored and serves 864 monthly observations from 1954-07; the `2026-07-24` vintage simply predated its registration by WP2.2 Task 1. The first seal recorded the consequence honestly (RFR-48: three missing factors, three uncomputable D4 strategies) but recorded it as a fact about the data rather than as a fact about the snapshot, and then sealed a whole campaign on it — removing `cash_tr_1m`, the `carry` D4 strategy, `term_premium`, `equity_risk_premium` and `policy_anchor_deviation` from Step 2 entirely. A live `ah data refresh --live` restored it, the campaign vintage moved to `2026-07-26.1`, and every band, floor, strategy statistic and measured claim was re-derived. The per-factor bands of the eleven pre-existing factors came back **bit-identical**, which is the check that the move touched only what depended on it. | n/a — resolved before merge, as amendments `AM-2026-07-26-003`/`-004` and decision `S2-CAMPAIGN-VINTAGE`. | Closed in the WP2.3 re-seal. **The general lesson, which is why this is a register row and not a note in a report:** a "declared available, no data" factor must be diagnosed as *snapshot vs source* before anything is sealed on it. `hy_spread` (an ICE licensing limit) and `commodities` (unsourced) are the other kind and are still open; `policy_rate` was not. |
| 2026-07-26 | RFR-62 | WP2.3 re-seal (found while re-running the refresh) | **`ah.data.refresh` silently DROPPED every fresh series from each new vintage.** A vintage is documented as a complete as-of snapshot and every read pins exactly one (`catalog.read_observations`/`read_asof` resolve one vintage id with no per-series fallback), but `refresh.plan` only fetches series that are *missing or stale* — so the first refresh after the initial build wrote only the due series and every fresh one vanished from pinned reads, reporting 0% coverage in `GAPS.md` while its observations sat on disk under the older vintage. Observed concretely: `fred.TEDRATE` (retired 2022-01, therefore never stale under its 9999-day SLA) fell out of the `2026-07-26` vintage, taking `funding_spread` with it. NOT deferred — **fixed in the same commit**: `Catalog.latest_vintage_with()` plus `refresh._carry_forward()` re-stamp every already-held, not-refetched series into the new vintage. Immutability is untouched (a new `(vintage, series)` key; the older vintage stays byte-identical) and carried rows are not re-submitted to QC, because re-judging unchanged history against a later as-of date would quarantine a vintage for the sole reason that a retired series is still retired. | n/a — resolved. It blocked this work package's own requirement that the campaign vintage be complete. | Closed here. **Follow-up for whoever owns Step 1:** every vintage built before this fix is incomplete in the same way, and `ah data status` reported those gaps as data gaps rather than as refresh gaps. `2026-07-26.1` is the first complete vintage; `2026-07-26` is superseded and must not be read. |
| 2026-07-26 | RFR-63 | WP2.3 re-seal (branch review, Critical) — **re-takes RFR-12, supersedes RFR-51's reasoning** | **RFR-51 chose option (b) because option (a) was impossible; on the new vintage it is possible, so the decision had to be re-taken on its merits.** It was, and the answer is still (b) — now on measured evidence rather than on impossibility (`scripts/measure_seal_evidence.py`, `momentum_cash_counterfactual`, sealed verbatim in `decisions.S2-NUMERAIRE-BIAS`). Option (a) — give `momentum`'s flat months (23.2% of them) the cash return instead of 0.0 — requires joining `equity_mkt` to `policy_rate`, which truncates momentum's historical sample from 1134 to 798 months, deleting 1926-1954 including the 1929-33 crash: ES99 falls 0.15597 → 0.12923, ES95 0.09102 → 0.07710. Measured on that same truncated span, the cash leg changes VaR95/ES95/VaR99/ES99 by **nothing at five decimals**; it moves only the mean, by about 1.2%/yr. So (a) corrects a mean-level bias that is structurally invisible to every statistic sealed for `momentum`, at the cost of the worst tail in the record. | n/a — decided, not deferred. | Closed in the WP2.3 re-seal. **Re-entry is now concrete rather than aspirational:** applying `ah.data.splice.PROXY_RULES['fedfunds_pre1954']` (backfill from `fred.TB3MS`, first observation 1934-01) shrinks the truncation from 336 months to about 90 and flips the trade — at which point (a) is the better option and the switch is a `threshold_change` amendment plus recomputed `momentum` bands. That also puts `splice.py` on the read path and therefore inside the seal (`seal_scope.splice_py`). |
| 2026-07-26 | RFR-64 | WP2.3 re-seal (branch review, Critical) — **closes RFR-9** | **`endowment_proxy`'s weights are sealed as a RISK BUDGET, not capital shares throughout.** RFR-9 asked WP2.3 to decide and the first seal did not: RFR-51 / `S2-NUMERAIRE-BIAS` covered the *portfolio-level* numeraire bias RFR-12 widened it into, but never answered RFR-9's own narrower question about what the 0.15 `credit_xs_hy` weight *is*. It is now answered in the sealed definition (`decisions.S2-ENDOWMENT-WEIGHTS`): `equity_mkt` 0.65 + `govt_tr_10y` 0.10 + `commodities` 0.10 are capital shares summing to 0.85, `credit_xs_hy` 0.15 is a notional on a self-financing duration-hedged position, and 0.15 of capital is uninvested. The alternative — a duration-matched government leg at HY spread duration 4.0y — was rejected because it authors a **new** sealed derived series with no evidence, for a strategy with no computable historical path on this vintage. | n/a — decided, not deferred. | Closed in the WP2.3 re-seal. If `hy_spread` ever acquires train+validation history (which needs the `hy_oas_pre1996` splice and a campaign restart), whoever recomputes its bands should revisit this with real data rather than inheriting the decision. |
| 2026-07-26 | RFR-65 | WP2.3 re-seal (branch review, Critical) | **The sealed multi-seed promotion rule's first clause named something the code cannot resolve.** It read "the challenger beats bootstrap-v1 on **the tail tier**" — but `ah.eval.battery.TIERS` is `(monthly, 1_5yr, 10yr, economic, severe)`, there is no `tail` tier, and every metric `ah.eval.metrics.tails.build_tails_suite` registers carries `tier="monthly"`. "Beats" was equally undefined. A promotion rule whose gating clause is unexecutable is not pre-registered at all: WP2.11 would have had to invent the definition **after seeing results**, which is exactly what sealing a decision rule exists to prevent. NOT deferred — **defined in the same commit**, as `multi_seed_decision_rule.tail_tier_definition` (the `tails` SUITE, not a horizon tier; the exact metric families; the comparison set restricted to computable D4 strategies) and `beats_definition` (a strict-inequality objective on mean `elicitability_score`, a no-tail-band-regression condition, an explicit NaN direction, and the pooled arm written as an exact inequality on the cross-seed mean and sd). | n/a — resolved. | Closed here, and machine-checked: `tests/test_prereg.py` asserts the sealed tail-tier definition against what a live `build_tails_suite` registration actually emits, so the sealed words and the code cannot drift. **WP2.11** implements the rule as written and may not reinterpret either definition. |
| 2026-07-26 | RFR-66 | WP2.3 re-seal (branch review, Critical) | **The head-to-head at G2 is biased TOWARD promotion by the benchmark's data window, not by either generator's quality.** `bootstrap-v1` can only resample `block_draw_span` (1990-2020; RFR-56), whose worst equity drawdowns are 2000-02, 2008-09 and 2020, while a challenger fitted on the full train+validation span has seen 1929-33, 1937, 1973-74 and 1987 — and both are scored against the *same* historical realizations, which include all of it. On any tail statistic that rewards reproducing the deep pre-1990 left tail (`elicitability_score` first among them) the benchmark is handicapped by its window rather than by its form. The direction matters: the plan's posture is that SHIP-BENCHMARK is a successful outcome, so this biases against the conservative verdict. NOT deferred — **sealed in the same commit** as `multi_seed_decision_rule.benchmark_draw_span_bias`. | n/a — recorded inside the hash rather than deferred; the *fix* is RFR-56's three routes and none is taken. | **WP2.11**: `G2-EVIDENCE.md` must report this alongside any PROMOTE verdict, and must report the challenger-vs-benchmark `elicitability_score` comparison restricted to the 1990-2020 realizations as well as on the full sample. Neither number gates — but a PROMOTE that survives only on the full sample and vanishes on the common window is a promotion of a data window. |
| 2026-07-26 | RFR-67 | WP2.3 re-seal (branch review, Important) | **Deleting `pre-registration.lock` made every battery run verify clean, silently.** `ah.eval.prereg.verify()` skipped the lock check entirely when the file was absent (`if lock_path is not None and lock_path.exists()`), so `rm pre-registration.lock` disarmed every hashed-source check with no error anywhere — the one failure mode the lock exists to make impossible, reachable with one command. Worse, a test *asserted the defect* (`test_verify_with_nonexistent_lock_path_skips_lock_check`). NOT deferred — **fixed in the same commit**: naming a `lock_path` for a **sealed** document now asserts the lock is there, and its absence is an error. An UNSEALED document legitimately has no lock (that is the pre-seal state `seal()` is called from), so only the sealed case errors, and passing no `lock_path` at all is still the supported way to run the structural checks alone. The test that asserted the defect now asserts the contract, with the reversal documented in its own docstring. | n/a — resolved. | Closed here. Two tests pin it: sealed-and-lock-missing raises, unsealed-and-lock-missing does not. |
| 2026-07-26 | RFR-68 | WP2.3 re-seal (branch review, Important) | **The sealed `ensemble_size.n_paths` was 1000; the MC-error grid it rests on was measured at 1024 — and the grid itself lived OUTSIDE the seal.** Two defects in one value. (1) A round number stood in for a measured one: nothing was ever measured at 1000, so the sealed criterion size was an interpolation presented as a derivation. **1024 is now sealed**, because 1024 is what the grid measures. (2) The grid lived in `governance/decision-register.md`, which is not hashed — so the evidence a sealed value rests on was editable with no amendment and no lock violation. It now lives in `pre-registration.yaml`'s `ensemble_size.mc_error_grid`, inside the hash, re-measured on the new campaign vintage at the sealed `mean_block_months`, with `scripts/measure_mc_error_grid.py` as its committed provenance script. | n/a — resolved. | Closed here. **WP2.4 and WP2.8–2.10 must produce criterion-bearing ensembles at 1024 × 120**, not 1000; a run at any other size records `criterion_bearing: false`. The general lesson: evidence for a sealed value belongs inside the seal, and a sealed number must be one that was actually measured. |
| 2026-07-26 | RFR-69 | WP2.3 re-seal (branch review, Minor) | **Two count errors in the sealed prose, both corrected, both worth recording because a sealed document's arithmetic is part of what it seals.** (a) `structurally_unavailable_statistics`' header said "All eleven names"; the block has always listed **twelve** (`ergodicity_gap`, three `regime_duration_*`, six `implied_sharpe_*`, two `ten_year_return_vs_valuation_*`) and RFR-55 says twelve. The LIST was never wrong — `tests/test_prereg.py` asserts it against a live suite registration in both directions — only this count of it. (b) `rationale.d4_commodities_consequence` said two of five D4 strategies were unevaluable "on the strength of `commodities` alone", which was wrong in substance even where its number was right: `endowment_proxy` is independently blocked by `hy_spread` via `credit_xs_hy` and would stay uncomputable if commodities were sourced tomorrow. | n/a — corrected in the same commit. | Closed here. |
| 2026-07-26 | RFR-70 | WP2.3 final pass (re-seal review, Critical 1) | **A sealed scope justification rested on a premise the campaign-vintage move had made false.** `seal_scope.splice_py_reason` read "policy_rate and hy_spread appear in `reference_run.missing_factors` precisely BECAUSE the backfills are absent". On vintage `2026-07-26.1` `missing_factors` is `[commodities, hy_spread]`: `policy_rate` is PRESENT, and what demonstrates its `fedfunds_pre1954` backfill is unapplied is its START DATE (`fred.FEDFUNDS`'s own 1954-07, not `fred.TB3MS`'s 1934-01). The correct wording already existed in `ah/eval/prereg.py`'s docstring, in `factors.yaml` and in `governance/decision-register.md`'s `S2-SEAL-SCOPE-2` -- it was simply never carried into the sealed block. The CONCLUSION (splice.py stays outside the seal) is unchanged and was never at risk: each rule is independently demonstrated unapplied. Carried in the same commit, with the false premise retained only as a labelled, quoted correction. `ah.eval.prereg.verify()`'s docstring travelled with it: it still said "if `lock_path` is given **and exists**", describing the pre-RFR-67 behaviour, inside the hash. | n/a -- corrected in the same commit, before merge. | Closed here. `tests/test_prereg.py` pins both directions (the sealed text must state `policy_rate`'s presence, and must not assert the old premise). |
| 2026-07-26 | RFR-71 | WP2.3 final pass (re-seal review, Critical 2) | **`criterion_bearing` claimed a check it did not perform, and the hazard was live.** `multi_seed_decision_rule.criterion_bearing_runs_only` names three conditions -- the sealed `ensemble_size`, the sealed `campaign_vintage_id`, and a verified pre-registration with a matching lock -- and points at `BatteryReport.criterion_bearing` as recording them. `ah.eval.battery` compared `n_paths` and `months` and nothing else; `ensemble.meta.vintage_id` was carried onto the report and never compared. Two incomplete predecessor vintages (`2026-07-24`, which has no `fred.FEDFUNDS`; `2026-07-26`, which has no `fred.TEDRATE` -- RFR-62) remain on disk and stay reachable through the catalog's append-only pointer history, and `catalog.asof('2026-07-25')`/`asof('2026-07-26')` both resolve to `2026-07-24`, so a 1024x120 run against either would have been stamped `criterion_bearing: true`. NOT deferred and NOT documented-around -- **the code was extended to meet the sealed sentence** (`ah.eval.battery.criterion_bearing_for`), because that makes the sentence true and closes the hazard at the only place it can bite. | n/a -- resolved. It is a judged source, so the seal moved regardless. | Closed here, pinned by `tests/test_eval_battery.py` and `tests/test_prereg.py` against the REAL sealed document. **WP2.11** still owes the hard refusal in `ah/eval/g2.py`; that half remains a sealed requirement, not a check in place. |
| 2026-07-26 | RFR-72 | WP2.3 final pass (re-seal review, folded in) | **`beats_definition` clause (ii) is structurally empty for one of its two metric families, and the seal did not say so.** Clause (ii) ("NO TAIL-BAND REGRESSION") is scoped to usable REFERENCE bands, and `ah.eval.reference.RegisteredStrategyStat` carries no `fn` by construction -- a backtest statistic or a scoring-rule value has no "history's own value of the same statistic" to band. So no bootstrap band exists, or can exist, for any of the eleven strategy statistics, and clause (ii) is evaluated ENTIRELY over the cross-block tail-dependence family (63 usable bands on this vintage, measured on WP2.4's own artifacts) -- ZERO strategy-level metrics. Read plainly, "over comparison-set metrics" suggested it covered `var_95`/`es_95`/`es_99`, which it never touches. DISCLOSURE ONLY: the clause is unchanged and still deterministic, and no threshold moved. | n/a -- disclosed in the same commit. | Closed here as disclosure. **The alternative is named in the sealed text and NOT taken:** writing clause (ii) against the sealed `thresholds.strategies` rather than against reference bands WOULD cover the strategy statistics. Changing it now, with WP2.4's benchmark numbers in hand, is post-hoc; a later work package may propose it as a dated `protocol_change` amendment, before any challenger is evaluated. |
| 2026-07-26 | RFR-73 | WP2.3 final pass (re-seal review, folded in) | **The lambda invariance argument does not survive incommensurable objectives.** `tuning_protocol.selection_criterion` argued `selection_lambda`'s level is immaterial because the same lambda applies to every system, sampler, seed and trial. But `generative_objective` is a different quantity per arm: ablation system D runs two samplers, `hier-diffusion-v1` and `hier-flow-v1`, whose objectives are an ELBO (a bound) and an exact log-likelihood, and the trial budget is per system PER SAMPLER, so the two are separately selected -- a fixed lambda therefore gives the D4 tail auxiliary a DIFFERENT EFFECTIVE WEIGHT per arm. `lambda = 1.0` remains fine as a pinned, never-refit constant, and no constant can repair this (any scale-aware value would have to be read off the trials, which the protocol forbids); the stated REASON was what overreached. The sealed text now says invariance is invariance of the RULE, not of the effective weight. | n/a -- corrected in the same commit; the VALUE is unchanged and was not re-fitted. | Closed here. **WP2.8** must report both terms of `S` separately, per system and per sampler, with their scales, so a reader can see what weight the auxiliary actually carried rather than inferring it from lambda. |
| 2026-07-26 | RFR-74 | WP2.4b claims sweep (sealed-claims audit) | **A defect class, not an incident: a sealed sentence that describes a check, unanswerable to the code.** RFR-70 (`splice_py_reason`) and RFR-71 (`criterion_bearing`) were both instances, both survived multiple review passes, and each was closed with its own single-purpose test -- but nothing generalised. The sweep audited every sentence in `pre-registration.yaml` and in the hashed sources that asserts something checkable, against the implementation and, where data-derived, against the sealed campaign vintage. **TEN more false claims at the time this row was written -- SEVEN in `pre-registration.yaml` (items 3-7 below plus the two accounting/tense defects named after them) and THREE in hashed SOURCE prose (items 1, 2 and `ah/splits.py`). None moved a verdict. RFR-75 adds an eleventh, also in source prose, and states the final partition:** (1) `factors.yaml`'s header still said `policy_rate` produced no train+validation data and called `missing_factors` "the sealed list of all three" -- the third surviving copy of RFR-70's sentence; (2) `ah.eval.prereg._check_threshold_data_availability`'s docstring said the same thing -- the fourth; (3) the thresholds header said `cross_block_corr_matrix_distance` "is the one entry today" for `thresholds.panel`, which seals 35 of `PANEL_STATS`' 49 names; (4) `conventions.estimator_length_matching` said THREE registered statistics carry `length_matched=False` and then named four (four registration records carry it); (5) `conventions.warm_up`'s "about 0.95 * 5% = 4.57%" does not multiply -- the factor is the DIFFERENTIAL warm-up fraction, 1 - (12/120 - 12/~800) = 0.915, and 4.57% and everything downstream of it were correct; (6) the Kupiec floor's stated implied interval `[0.0042, 0.1125]` ("about 0.5 or 13.5 exceedances") is not the LR = 6.635 contour, which is `[0.008328, 0.108796]`, counts `[1.00, 13.06]`; (7) `ensemble_size.mc_error_grid.reading` attributed the ~3.8-fold 64->1024 reduction to `skew` and `acf_abs_sum` (4.52x and 5.60x in the sealed table) rather than to `ust_10y.acf_r_lag1` (3.75x) and `excess_kurtosis` (3.69x). **Plus two accounting/tense defects, both in `pre-registration.yaml`:** `seal_scope` calls this file's header and `ah.eval.prereg`'s docstring the full accounting of what is hashed, while `src/ah/eval/metrics/_pooling.py` and `src/ah/eval/negative_controls.py` were named in neither (both always hashed -- the accounting was short, never the seal); and `tuning_protocol.prohibitions` sealed WP2.7's filter bar in the present tense ("asserted by test against this manifest") when no such test exists or can yet exist. `ah/splits.py` also still called the sealed splits "provisional". **NOT FOUND, stated because a clean result is a result:** every threshold's `K = 4` absurdity arithmetic reconciles exactly with its quoted band; every strategy bound is exactly `[historical/3, historical*3]`; the sealed D4 strategy statistics, the memorization null (n = 367, p05 0.0557, p50 2.0742), the spread-floor evidence (ig_spread 54.0% of 1224, min 0.32; funding_spread 86.9% of 420, min 0.118) and the RFR-12 momentum counterfactual all reproduce to every digit via `scripts/measure_seal_evidence.py`; `missing_factors` is `[commodities, hy_spread]`; the coverage table is internally consistent month for month; the Markov/Hoeffding/binomial/chi-square derivations all check out; and every `verify()`, loader and `run_battery` behavioural claim holds against the implementation. | n/a -- all nine corrected in the same commit. No threshold, band, gate, floor, split, size or severity moved; the only CODE changes are comments and docstrings plus new tests. | Closed here, and **generalised**: `pre-registration.yaml` now seals a `claims_with_tests:` registry mapping every claim-shaped sealed line to the test that pins it, with a sealed trigger-phrase detector and three tests (`test_every_sealed_claim_is_pinned_by_a_test_that_exists`, `test_every_sealed_claim_anchor_still_matches_its_sentence`, `test_no_claim_shaped_sentence_is_unregistered`). **Its limits are sealed beside it and are real:** it is a keyword detector, not a reader (a claim phrased without a trigger phrase is invisible); it checks that the named test EXISTS, not that it asserts the claim; and it covers `pre-registration.yaml` only -- four of this pass's findings were in hashed SOURCE prose, which remains uncovered. **Extending the scan to the hashed sources is the obvious next step and is unowned.** |
| 2026-07-26 | RFR-75 | WP2.4b claims sweep (continued, after RFR-74 was written) | **An ELEVENTH finding, and RFR-74's own count of the sweep is superseded by this row rather than rewritten** (this register is append-only; the rule is to add a row referencing the earlier `id`, never to edit it). RFR-74 recorded seven false claims in `pre-registration.yaml` plus three in hashed SOURCE prose (`factors.yaml`, `ah/eval/prereg.py`, `ah/splits.py`). A further sweep of the hashed sources -- run after RFR-74 was appended, checking every `tests/test_*.py::test_*` reference in a hashed module against the AST of the file it names -- found one more: `ah/eval/negative_controls.py`'s module docstring cites `tests/test_negative_controls.py::test_finding_the_monthly_tier_cannot_separate_nc3_from_the_undistorted_bootstrap` "for the exact, paired comparison this claim now rests on". **That test does not exist.** WP2.2c Item 1 closed the finding and replaced it with `test_closed_every_suite_now_emits_mean_std_and_correlation` and `test_closed_the_monthly_tier_separates_nc3_from_the_undistorted_bootstrap`; the citation was never updated. The same docstring also states the suite's own convention as "when WP2.3 closes one, that test fails loudly and **is deleted** in the same commit" -- which is not what the suite does and not what was done here: closed findings are REPLACED by a `test_closed_*` so the historical record survives beside the proof it no longer holds. A description of the repository's own practice is a claim too. **The correct total for the sweep is therefore ELEVEN findings: seven in `pre-registration.yaml`, four in hashed source prose.** Nothing in this row moves a verdict: the cited test's absence has no runtime effect, and the finding it pins is a historical record that is explicitly closed. | n/a -- corrected in the same commit. No threshold, band, gate, floor, split, size or severity moved in this row either. | Closed here. **This finding is the direct evidence for RFR-74's own stated limit:** the sealed `claims_with_tests` registry covers `pre-registration.yaml` only, and this defect sat in hashed SOURCE prose, where the standing check does not look. It was caught by a one-off script, not by anything that will run again. **The mechanical form of that missing check is now known and cheap** -- for every hashed `.py`, resolve every `tests/<file>::<test_name>` it names against that file's AST, exactly as the one-off did -- and extending `claims_with_tests` to hashed-source prose remains UNOWNED. Whoever takes it should start there. |
| 2026-07-29 | RFR-76 | WP2.10 (found while extracting the ablation grid's results) — **the first POST-HOC amendment in the project** | **A sealed prose gloss named the COMPLEMENT of the sealed field it glossed, and the two readings give opposite G2 verdicts.** `multi_seed_decision_rule.tail_tier_definition` defined the comparison set as "the five `d4_strategies` minus `reference_run.uncomputable_d4_strategies`, **which on this vintage is `sixty_forty`, `momentum` and `carry`**". The field is `[eqw_factors, endowment_proxy]`. Reading the FIELD (what `ah/eval/ablation.py` actually does): the comparison set is the three computable strategies, the rule executes, and on the WP2.10 grid `hier-flow-v1` satisfies clause (1) in all three seeds. Reading the GLOSS literally: the comparison set is the two strategies that are NaN on the generated side in all 18 cells, and `beats_definition`'s NaN rule then makes **no seed a beat for any system, ever** — an automatic SHIP-BENCHMARK independent of all evidence. Corrected by `AM-2026-07-29-001` (`post_hoc: true`), which adopts the field's reading; no threshold, band, gate, floor, split, size, severity, selection weight or rule text moved, and no code changed. **THE STANDING CHECK COULD NOT HAVE CAUGHT IT.** `tests/test_prereg.py::test_the_sealed_tail_tier_definition_matches_the_registered_statistics` asserts that the definition names the eleven per-strategy and two per-pair statistics, that `uncomputable_d4_strategies` is a proper subset of `d4_strategies`, and that the subtraction is non-empty — it never compares the prose's ENUMERATION of strategies against the field's VALUE, and all five strategy ids appear elsewhere in the same folded block, so no substring test would have separated them. `claims_with_tests` does not reach it either: the clause carries none of the sealed `trigger_phrases`, and this block's two registered anchors sit on the following two lines. | n/a — corrected in the same working tree, with a dated amendment and a re-seal (`sha256:2531904623db…` → `sha256:e5a669cf156a…`; hashed file set unchanged, 32 files). What is deferred is the GENERAL check, not this instance. | **UNOWNED.** The defect class is the claims sweep's (RFR-74/-75) one level down: a sealed sentence that restates a sealed FIELD's value in prose, with nothing checking the restatement against the field. The mechanical form is known — for each sealed prose block that names a document field, resolve the named field and compare the enumerated values — but a general version needs a way to detect "this prose is restating that field", which `claims_with_tests`'s keyword detector cannot do. Minimum viable step, and the one a later WP should take first: a targeted test that `tail_tier_definition`'s comparison-set clause names exactly `reference_run.uncomputable_d4_strategies`'s members and no others. Not built inside this amendment, deliberately — a correction made with results in hand should change as little as possible. |
| 2026-07-30 | RFR-77 | WP2.11 governance step (decision `S2-SEVERE-GATING`) | **A sealed protocol that pins a PROCEDURE but no CRITERION -- an omission, where RFR-74/-76 were false statements.** `severe_test_protocol` specifies exactly what to exclude, what to refit, what to retrain frozen, where to launch from and what window to compare, and it requires the result to be written up either way. It never says what counts as passing. WP2.11 part 1 therefore returned a reading (INCONCLUSIVE) that no sealed rule produced, and the owner closed the gap the only way that does not author a threshold after seeing the score: by recording the reading as an explicitly-labelled human judgement and treating the severe test as reported evidence rather than as a gate (`governance/decision-register.md`, `S2-SEVERE-GATING`). **The defect is not that the test was run wrong; it is that the seal could not have adjudicated it however it came out.** The four clauses of `multi_seed_decision_rule.rule` are unaffected -- the severe test is not one of them and never was -- so nothing about the G2 verdict arithmetic turns on this row. | Not fixable here without doing the thing the seal exists to prevent. Any threshold written now would be written by the party holding the result, after seeing it, for a test whose contrast is a median 6.4% of a gap that exists in both arms -- i.e. a number chosen knowing which side of it the answer falls. Deferring is the conservative act, not the lazy one. | **UNOWNED.** Minimum viable step for the next campaign's seal, and it generalises past this one protocol: **every sealed protocol whose result may be cited in `G2-EVIDENCE.md` must either pin a decision threshold or state in the sealed text that it is non-gating** -- and a test should assert that partition holds for every `*_protocol` key in the document, so an unadjudicable protocol cannot be sealed silently again. Not built now: a correction made with results in hand should change as little as possible (the same restraint `AM-2026-07-29-001`/RFR-76 recorded). |
| 2026-07-30 | RFR-78 | WP2.11 governance step (decision `S2-HORIZON-TIER`) | **A sealed sentence names a tier the code does not define.** `severe_test_protocol` says to compare 1966-1984 "through the horizon tier"; `TIERS` has no `horizon` tier. Two defensible resolutions exist and they are not the same set: `suite == "horizon"` selects 110 metrics, `tier in {1_5yr, 10yr}` selects 113, differing by `interval_coverage_50_5y`, `interval_coverage_90_5y` and `pit_ks_stat_5y`. This is `AM-2026-07-29-001`'s defect class one step over -- there, sealed prose restated a sealed FIELD's value wrongly; here, sealed prose names a CODE-LEVEL identifier that does not resolve. Neither reading was adopted: WP2.11 part 1 reports both with every row carrying its suite and tier, and `S2-HORIZON-TIER` makes that permanent for this campaign. | Choosing between them with results in hand is a forking path, and "they agree anyway" is not a claim available before looking. A `protocol_change` amendment narrowing the phrase is legitimate **before the next campaign, when no results are in view** -- and illegitimate now, for the same reason a threshold cannot be authored in RFR-77's row. | **UNOWNED**, two parts. (1) The narrowing itself: a dated `protocol_change` amendment pinning one reading, taken pre-campaign. (2) The general check, which is cheap and mechanical and would have caught this at seal time: **every tier, suite and metric identifier appearing in sealed prose must resolve against the live `TIERS`/`SUITES` registries**, asserted by test. That closes a gap `claims_with_tests` cannot -- it verifies a named test EXISTS, not that a named IDENTIFIER resolves. Whoever takes RFR-76's targeted test should take this in the same pass; they are the same shape. |
| 2026-07-30 | RFR-79 | WP2.11 governance step (**assigns RFR-42, which has been UNSCHEDULED and unowned since 2026-07-26**) | **The `10yr` tier's 73% structural unavailability now has an owner and a target, which is what RFR-42 asked for and did not get.** RFR-42 recorded that 16 of 22 `10yr`-tier metrics cannot be computed, named all three causes, and said the missing valuation factor "should be given an owner before G2". Two of the three causes have since been discharged by the work itself (regime duration arrived with WP2.6; `ergodicity_gap`'s multi-century path is within WP2.5's climate model). **The valuation factor is the one that remains, and it is a Step-1 DATA item, not a Step-2 modelling one:** a new `requirements.yaml` series plus a `factors.yaml` mapping plus a connector, none of which exists. Assigned: **owner = project owner**; **target = a new Step-1 work package `WP1.13` (valuation series connector), following `Instructions/WP1.12-UK-CONNECTORS.md`'s precedent**, to land before Step 3's decade-scale work needs it. | Not deferred further and explicitly NOT closed by this row -- assignment is not resolution. It cannot be closed before G2 because the blocker is a missing INPUT, and inventing a proxy for a factor that carries 0.20 of `eqw_factors` and 0.10 of `endowment_proxy` is exactly the silent substitution this project refuses (RFR-8's reasoning, unchanged). | **`WP1.13`, owner: project owner.** Until it lands, G2 proceeds with the `10yr` tier declared UNAVAILABLE, exactly as `conventions.ten_year_tier_coverage` seals it and as RFR-42 requires -- and `G2-EVIDENCE.md` may not cite a `10yr` pass. This row exists so that a reader tracing RFR-42 forward finds a destination instead of the word "unscheduled". |
| 2026-07-30 | RFR-80 | WP2.11 governance step (**corrects RFR-79, appended the same day; this register is append-only so the defective row stands and this one supersedes it**) | **RFR-79 claimed that two of RFR-42's three causes "have since been discharged by the work itself". THAT CLAIM IS NOT ESTABLISHED, and for one of the two it is contradicted by the current tree.** (c) `ergodicity_gap`, 14 of the 16 unavailable metrics: the sealed `conventions.ten_year_tier_coverage` says the multi-century path "is a generator capability that **WP2.5's climate model COULD supply and no current generator does**" -- a possible route, which RFR-79 restated as a delivered one. `src/ah/eval/negative_controls.py`, a HASHED judged source in the tree at this commit, still states 16 of 22 metrics unavailable including all 14 `ergodicity_gap` "with no reference band at all". **So (c) is open.** (b) the regime-duration inputs: `usrec` and `growth_yoy` exist in `ah/data/derive.py`, but as Step-1 features for regime LABELLING, not as generated factors -- nothing verified here shows WP2.6 made the regime-duration mapping computable over GENERATED output, so the honest status is UNVERIFIED, neither discharged nor known open. **The correct reading of RFR-79 is therefore: one cause (the valuation factor) assigned to `WP1.13`; the other two NOT closed by that row and not closed by this one.** Nothing else in RFR-79 moves -- the assignment, the owner, the target and the "assignment is not resolution" caveat all stand, and no verdict, threshold or sealed value is touched by either row. | n/a -- corrected within the day, by appending rather than editing, per this file's own rule ("if a deferred item is later resolved, add a new row ... rather than rewriting history here"). The defect is a governance-prose overclaim with no runtime effect: nothing reads these rows, and `G2-EVIDENCE.md` was already forbidden from citing a `10yr` pass by the seal itself, independently of what RFR-79 said. | **UNOWNED for (b) and (c).** (b) needs a check, not a build: determine whether the regime-duration 10yr mapping is computable over generated output post-WP2.6, and record the answer -- cheap, and it should precede any claim either way. (c) needs a generator capability (emit one multi-century path) that no work package currently owns; `WP2.5`'s climate model remains the plausible home, as the seal says, and saying so is not the same as scheduling it. **The general lesson, which is this row's real content: a governance row that summarises a SEALED disclosure must quote its modality.** "could supply and no current generator does" and "is supplied" differ by exactly the word this defect turned on -- the same failure mode as RFR-76 (a gloss that misstates the thing it glosses), now committed in the register rather than in the seal. |
| 2026-07-30 | RFR-81 | WP2.11 governance step (owner supplied the CAPE and a recession series; **supersedes RFR-42's, RFR-79's and RFR-80's account of cause (a), and the SEALED disclosure's account of it too**) | **THE VALUATION DATA WAS NEVER MISSING. Only the FACTOR MAPPING is.** `conventions.ten_year_tier_coverage` says closing cause (a) needs "a new `requirements.yaml` series plus a `factors.yaml` mapping"; RFR-42 and RFR-79 repeated that framing and RFR-79 built a whole work package (`WP1.13`, "valuation series connector") on it. **All three are wrong on the first half.** `shiller.cape` has been registered in `requirements.yaml` since Step 1 (line 49: FREE, monthly, `min_start: 1881-01`, `intake: auto`, P0) and `src/ah/data/connectors/shiller.py`'s `_COLMAP` already parses the `CAPE` column from Shiller's `ie_data.xls` -- the same workbook the owner supplied, now at `data/ie_data.xls`. The recession indicator is registered too: `fred.USREC`, `requirements.yaml` line 37, monthly 0/1 from 1854-12, `enforce: true`. (`SAHMREALTIME`, which the owner also supplied, is NOT registered and is a genuinely different series -- a real-time rule without USREC's revision lag -- so it is an addition worth making on its merits, not a substitute for something absent.) **THE REAL BLOCKER, which every prior row mis-located:** `factors.yaml`'s active blocks are `global` (8) + `us` (6) = 14 factors and none is a valuation factor. Adding a fifteenth GENERATED factor is a factor amendment plus a full retrain of L1, L2 and L3 -- i.e. WP2.5 through WP2.10 redone -- for the reason already sealed for `R5` (FX) and `J3` (UK): cross-block correlation cannot be grafted onto trained weights. The metric needs a valuation path the GENERATOR emits, not a historical series, which is why registering data does not close it. | **DECISION OPEN, owner's call, deliberately not pre-empted here.** Two routes. (1) Complete G2 on 14 factors with the `10yr` tier declared UNAVAILABLE exactly as sealed, and enter the valuation factor as a dated amendment plus retrain for the NEXT campaign. (2) Weigh the retrain now -- which restarts the campaign, and would do so with the WP2.10 head-to-head already in hand, the same hazard `AM-2026-07-29-001` already cost. The recorded recommendation is (1); it has not been taken as a decision because it is not this row's to take. | **`WP1.13` is rescoped by this row and shrinks substantially**: not "build a connector" but "map `shiller.cape` (and optionally register `fred.SAHMREALTIME`) into `factors.yaml` as a generated factor, then retrain". It stays owned by the project owner. Until it lands, cause (a) is open, the `10yr` tier stays UNAVAILABLE per the seal, and `G2-EVIDENCE.md` may not cite a `10yr` pass -- unchanged by this row. **The lesson, third in a row and now pointing INTO the seal:** RFR-76 was a sealed gloss misstating a sealed field; RFR-80 was a register row misquoting a sealed modality; this is a SEALED sentence misdescribing the repository's own data layer, uncaught because `claims_with_tests` checks that a named test exists, not that a stated fact about `requirements.yaml` is true. A check that resolves every series and factor name appearing in sealed prose against `requirements.yaml`/`factors.yaml` would have caught it, and belongs with RFR-78's identical proposal for `TIERS`/`SUITES`. |

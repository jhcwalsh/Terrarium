# Data (never committed — see CLAUDE.md / STEP1 licensing discipline)
data/

# Python
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.ruff_cache/
.mypy_cache/
.hypothesis/
htmlcov/
.coverage
.coverage.*
coverage.xml

# Environments
.venv/
venv/
env/

# uv
# (uv.lock IS committed; the venv is not)

# IDE / OS
.idea/
.vscode/
*.swp
.DS_Store
Thumbs.db

# Secrets — never commit
.env
.env.*

# Step 2 experiments (local-first; manifests may be kept manually)
experiments/
.superpowers/
